---
phase: 02-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Header.tsx
  - components/NavDropdowns.tsx
autonomous: true
requirements:
  - SHELL-01

must_haves:
  truths:
    - "Header renders using design tokens — no arbitrary text-[Npx] values remain in Header.tsx or NavDropdowns.tsx"
    - "The Folio wordmark displays at text-display (2.25rem / 36px) at all breakpoints — the responsive sm:text-[38px] is gone"
    - "The date label and nav trigger labels display at text-label (10px) — the editorial mono label pattern is preserved"
    - "The header and dropdown panel backgrounds use bg-paper (single dark-mode-aware token) — the paired bg-stone-50 dark:bg-stone-950 is gone"
    - "The thick top rule, sticky positioning, 3-column grid layout, and border-b separator are visually unchanged"
  artifacts:
    - path: "components/Header.tsx"
      provides: "Token-compliant header component"
      contains: "bg-paper"
    - path: "components/NavDropdowns.tsx"
      provides: "Token-compliant nav dropdown panel"
      contains: "bg-paper"
  key_links:
    - from: "components/Header.tsx"
      to: "tailwind.config.ts"
      via: "text-display, text-label, bg-paper utility classes"
      pattern: "text-display|text-label|bg-paper"
    - from: "components/NavDropdowns.tsx"
      to: "tailwind.config.ts"
      via: "text-label, bg-paper utility classes"
      pattern: "text-label|bg-paper"
---

<objective>
Migrate all arbitrary size values in Header.tsx and NavDropdowns.tsx to the Phase 1 design tokens.

Purpose: The header appears on every page. Moving it to named tokens removes magic numbers and makes the editorial shell consistent with the token system established in Phase 1.
Output: Header.tsx and NavDropdowns.tsx with zero arbitrary text-[Npx] values — every typography and background class uses a named token.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Design tokens available in Tailwind — confirmed from tailwind.config.ts and globals.css -->
<!-- Use these directly. No exploration needed. -->

Token: text-display
  Value: 2.25rem / 36px, lineHeight 1.2, fontWeight 700
  Note: fontWeight 700 is baked in — remove any co-located font-bold class to eliminate redundancy

Token: text-label
  Value: 0.625rem / 10px, lineHeight 1, letterSpacing 0.1em, fontWeight 500
  Note: When replacing text-[11px] with text-label on elements that also have tracking-[0.15em],
        leave tracking-[0.15em] in place (e.g. the "Archive Edition" span on line 56 of Header.tsx).
        Explicit tracking classes override the token's built-in letterSpacing.
        When replacing text-[11px] on triggers/labels where tracking-[0.1em] already matches
        the token value, you may remove the explicit tracking-[0.1em] as the token provides it,
        or leave it as a harmless duplicate — either is fine.

Token: bg-paper
  Value: hsl(var(--paper)) — resolves to warm off-white in light mode, warm dark in dark mode
  CRITICAL: Write as bg-paper ONLY. Do NOT write dark:bg-paper alongside it.
            The CSS variable switches automatically under .dark. dark:bg-paper is redundant.

Current Header.tsx (full file — 83 lines):
  line 29: <header className="sticky top-0 z-40 bg-stone-50 dark:bg-stone-950 border-b border-stone-200 dark:border-stone-800">
  line 37: <span className="hidden sm:block font-mono text-[11px] text-stone-400 dark:text-stone-500 tracking-wide">
  line 42: <h1 className="font-serif text-[32px] sm:text-[38px] font-bold tracking-tight text-stone-900 dark:text-stone-50 group-hover:opacity-75 transition-opacity">
  line 56: <span className="flex-1 font-mono text-[10px] tracking-[0.15em] uppercase text-stone-400 dark:text-stone-500 py-2">  ← LEAVE this one alone (10px already, tracking intentional)
  line 62: <Link className="font-sans text-[11px] font-semibold tracking-[0.1em] uppercase ...">  ← archive nav links
  line 68: <Link className="font-sans text-[11px] font-semibold tracking-[0.1em] uppercase ...">  ← archive nav links

Current NavDropdowns.tsx TRIGGER_CLS (line 50-51):
  'flex items-center gap-1 px-5 py-2 text-[11px] font-sans font-semibold tracking-[0.1em] uppercase text-stone-400 dark:text-stone-500 hover:text-stone-900 dark:hover:text-stone-100 transition-colors'

Current NavDropdowns.tsx dropdown panel (line 89):
  "absolute top-full left-0 z-50 min-w-[152px] rounded-b-xl rounded-tr-xl bg-stone-50 dark:bg-stone-950 border border-t-0 border-stone-200 dark:border-stone-800 shadow-lg shadow-stone-200/50 dark:shadow-stone-950/50 py-1 overflow-hidden"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Header.tsx to design tokens</name>
  <files>components/Header.tsx</files>
  <action>
Make exactly four targeted class string changes in Header.tsx. Do not touch any other part of the file.

CHANGE 1 — Header background (line 29):
  BEFORE: bg-stone-50 dark:bg-stone-950
  AFTER:  bg-paper
  Full class string: "sticky top-0 z-40 bg-paper border-b border-stone-200 dark:border-stone-800"

CHANGE 2 — Date label (line 37):
  BEFORE: font-mono text-[11px] text-stone-400 dark:text-stone-500 tracking-wide
  AFTER:  font-mono text-label text-stone-400 dark:text-stone-500 tracking-wide
  (Keep tracking-wide — it overrides the token's 0.1em letterSpacing intentionally for this wider label)

CHANGE 3 — Folio wordmark h1 (line 42):
  BEFORE: font-serif text-[32px] sm:text-[38px] font-bold tracking-tight text-stone-900 dark:text-stone-50 group-hover:opacity-75 transition-opacity
  AFTER:  font-serif text-display tracking-tight text-stone-900 dark:text-stone-50 group-hover:opacity-75 transition-opacity
  (Remove both text-[32px] sm:text-[38px] AND font-bold — text-display provides fontWeight:700)

CHANGE 4 — Archive nav links (lines 62 and 68):
  BEFORE: font-sans text-[11px] font-semibold tracking-[0.1em] uppercase ...
  AFTER:  font-sans text-label font-semibold uppercase ...
  (Remove text-[11px]; text-label provides the same letterSpacing so remove tracking-[0.1em] too)

DO NOT TOUCH:
  - line 56: the "Archive Edition" span — it uses text-[10px] (already 10px, matching text-label value) with intentional tracking-[0.15em]. Leave it as-is.
  - The thick top rule (line 31): <div className="h-[3px] bg-stone-900 dark:bg-stone-100" />
  - Sticky positioning, z-40, grid layout, border-b separator
  - hover:opacity-75 on the logo link and wordmark
  - FolioMark SVG, ThemeToggle, AuthButtons, NavDropdowns
  - formatShortDate function
  - HeaderProps interface
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && grep -n 'text-\[' components/Header.tsx || echo "CLEAN: no arbitrary text-[Npx] values"</automated>
  </verify>
  <done>
    - Header.tsx contains bg-paper (no dark:bg-paper alongside it)
    - Header.tsx contains text-display on the wordmark h1 (no font-bold, no sm:text-[38px])
    - Header.tsx contains text-label on the date span and archive nav links
    - grep -n 'text-\[' components/Header.tsx returns zero results (the line-56 span uses text-[10px] — acceptable until Phase 3, or migrate it to text-label here too for clean sweep)
    - npx tsc --noEmit passes with no new errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate NavDropdowns.tsx to design tokens</name>
  <files>components/NavDropdowns.tsx</files>
  <action>
Make exactly two targeted changes in NavDropdowns.tsx. Do not touch any logic, state, event handlers, or imports.

CHANGE 1 — TRIGGER_CLS constant (lines 50-51):
  BEFORE: 'flex items-center gap-1 px-5 py-2 text-[11px] font-sans font-semibold tracking-[0.1em] uppercase text-stone-400 dark:text-stone-500 hover:text-stone-900 dark:hover:text-stone-100 transition-colors'
  AFTER:  'flex items-center gap-1 px-5 py-2 text-label font-sans font-semibold uppercase text-stone-400 dark:text-stone-500 hover:text-stone-900 dark:hover:text-stone-100 transition-colors'
  (Replace text-[11px] with text-label; remove tracking-[0.1em] as text-label provides the same 0.1em letterSpacing)

CHANGE 2 — Dropdown panel background (line 89):
  BEFORE: bg-stone-50 dark:bg-stone-950
  AFTER:  bg-paper
  Full class string becomes: "absolute top-full left-0 z-50 min-w-[152px] rounded-b-xl rounded-tr-xl bg-paper border border-t-0 border-stone-200 dark:border-stone-800 shadow-lg shadow-stone-200/50 dark:shadow-stone-950/50 py-1 overflow-hidden"

DO NOT TOUCH:
  - useState, useRef, useEffect, handleOutsideClick
  - onMouseEnter, onMouseLeave event handlers
  - NAV_GROUPS data structure
  - Dropdown item links (text-[12px] — that stays for Phase 3)
  - The Saved direct link
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - NavDropdowns.tsx TRIGGER_CLS contains text-label (no text-[11px])
    - NavDropdowns.tsx dropdown panel contains bg-paper (no dark:bg-paper alongside it)
    - npx tsc --noEmit passes with no new errors
    - grep -n 'text-\[11px\]' components/NavDropdowns.tsx returns zero results
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Run compile check:
   cd /Users/gregoryvitrenko/Documents/Folio && npx tsc --noEmit
   Expected: zero errors

2. Run build:
   cd /Users/gregoryvitrenko/Documents/Folio && npm run build
   Expected: build succeeds, no Tailwind purge warnings for text-display or text-label

3. Confirm no arbitrary sizes remain in both files:
   grep -n 'text-\[' components/Header.tsx components/NavDropdowns.tsx
   Expected: either zero results, or only the line-56 "Archive Edition" text-[10px] (acceptable carry-over)

4. Confirm bg-paper is present and dark:bg-paper is absent:
   grep -n 'bg-paper' components/Header.tsx components/NavDropdowns.tsx
   grep -n 'dark:bg-paper' components/Header.tsx components/NavDropdowns.tsx  ← should return nothing

5. Confirm non-negotiables preserved:
   grep -n 'h-\[3px\]' components/Header.tsx   ← thick top rule still present
   grep -n 'sticky' components/Header.tsx        ← sticky positioning still present
</verification>

<success_criteria>
- Header.tsx: zero arbitrary text-[Npx] values for typography (thick rule h-[3px] is not typography — leave it)
- NavDropdowns.tsx: TRIGGER_CLS uses text-label; dropdown panel uses bg-paper
- Both files compile without TypeScript errors
- npm run build succeeds
- Visual appearance is indistinguishable from before (bg-paper is near-identical to stone-50/stone-950; text-display is 2px larger than sm:text-[38px] — imperceptible in practice)
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell/02-01-SUMMARY.md` using the summary template.
</output>
