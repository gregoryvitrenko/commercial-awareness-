---
phase: 02-shell
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/SiteFooter.tsx
  - app/layout.tsx
autonomous: true
requirements:
  - SHELL-02

must_haves:
  truths:
    - "A footer appears on every page with exactly five links in order: Feedback, Terms, Privacy, Contact, LinkedIn"
    - "Feedback and Contact links use mailto:hello@folioapp.co.uk — they do NOT open in a new tab"
    - "LinkedIn link opens in a new tab with rel=noopener noreferrer"
    - "The footer never overlaps page content — it stays pinned at the bottom of short pages"
    - "Footer typography uses text-label (token) — no arbitrary text-[10px] values remain"
  artifacts:
    - path: "components/SiteFooter.tsx"
      provides: "Five-link footer with token-compliant typography"
      contains: "LinkedIn"
    - path: "app/layout.tsx"
      provides: "flex flex-col body + main flex-1 children wrapper for sticky footer"
      contains: "flex-col"
  key_links:
    - from: "app/layout.tsx"
      to: "components/SiteFooter.tsx"
      via: "flex flex-col on body + main flex-1 wrapper makes mt-auto effective"
      pattern: "flex flex-col|flex-1"
---

<objective>
Complete the site footer with all required links and fix the sticky-footer layout so it stays pinned at the bottom of short pages.

Purpose: The footer appears on every page. Completing its links finishes a priority backlog item and makes the shell consistent. The flex layout fix makes mt-auto work — without it the footer floats mid-page on short content.
Output: SiteFooter.tsx with five links in the correct order; app/layout.tsx with flex flex-col on body and main flex-1 wrapping children.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current SiteFooter.tsx — 38 lines total -->
<!-- Footer has three existing links: Terms (/terms), Privacy (/privacy), Contact (mailto:hello@folioapp.co.uk) -->
<!-- Footer uses font-mono text-[10px] — migrate all to text-label for token consistency -->
<!-- SiteFooter already has mt-auto on <footer> — the layout.tsx fix makes this effective -->

Current footer nav (lines 14-33):
  <nav className="flex items-center gap-6">
    <Link href="/terms" className="font-mono text-[10px] text-stone-400 dark:text-stone-600 hover:text-stone-700 dark:hover:text-stone-300 transition-colors tracking-wide">
      Terms
    </Link>
    <Link href="/privacy" className="font-mono text-[10px] ...">Privacy</Link>
    <a href="mailto:hello@folioapp.co.uk" className="font-mono text-[10px] ...">Contact</a>
  </nav>

Target footer nav (5 links, order: Feedback · Terms · Privacy · Contact · LinkedIn):
  - Feedback: <a href="mailto:hello@folioapp.co.uk"> — no target="_blank"
  - Terms: <Link href="/terms"> — existing, keep as Link
  - Privacy: <Link href="/privacy"> — existing, keep as Link
  - Contact: <a href="mailto:hello@folioapp.co.uk"> — existing, no target="_blank"
  - LinkedIn: <a href="https://www.linkedin.com/in/gregory-vitrenko-7258a0350" target="_blank" rel="noopener noreferrer">

Token to use on all links: text-label (replaces text-[10px] — same 10px value, named token)
Class pattern for all links: "font-mono text-label text-stone-400 dark:text-stone-600 hover:text-stone-700 dark:hover:text-stone-300 transition-colors tracking-wide"

Current copyright line (line 10):
  <p className="font-mono text-[10px] text-stone-400 dark:text-stone-600 tracking-wide">
  Migrate to: <p className="font-mono text-label text-stone-400 dark:text-stone-600 tracking-wide">

Current layout.tsx body (line 58):
  <body className="bg-stone-50 dark:bg-stone-950 text-stone-900 dark:text-stone-100 font-sans min-h-screen transition-colors duration-200">

Current layout.tsx Providers block (lines 59-62):
  <Providers>
    {children}
    <SiteFooter />
  </Providers>

Required layout.tsx body (add flex flex-col):
  <body className="bg-stone-50 dark:bg-stone-950 text-stone-900 dark:text-stone-100 font-sans min-h-screen flex flex-col transition-colors duration-200">

Required layout.tsx Providers block (wrap children in main):
  <Providers>
    <main className="flex-1">
      {children}
    </main>
    <SiteFooter />
  </Providers>

CRITICAL — Do NOT pass className to Providers:
  Providers is a client component (app/providers.tsx). It does not forward className.
  Add flex-1 to a <main> element inside layout.tsx instead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete SiteFooter.tsx with all five links and text-label tokens</name>
  <files>components/SiteFooter.tsx</files>
  <action>
Rewrite SiteFooter.tsx with the following exact structure. The footer's outer markup (border-t, bg-stone-50 dark:bg-stone-950, mt-auto, max-w-5xl container, flex layout) stays unchanged. Only the nav contents and text-[10px] → text-label migration change.

The complete updated SiteFooter.tsx:

```tsx
import Link from 'next/link';

export function SiteFooter() {
  const year = new Date().getFullYear();

  return (
    <footer className="border-t border-stone-200 dark:border-stone-800 bg-stone-50 dark:bg-stone-950 mt-auto">
      <div className="max-w-5xl mx-auto px-4 sm:px-6 py-5 flex flex-col sm:flex-row items-center justify-between gap-3">

        <p className="font-mono text-label text-stone-400 dark:text-stone-600 tracking-wide">
          © {year} Folio
        </p>

        <nav className="flex items-center gap-6">
          <a
            href="mailto:hello@folioapp.co.uk"
            className="font-mono text-label text-stone-400 dark:text-stone-600 hover:text-stone-700 dark:hover:text-stone-300 transition-colors tracking-wide"
          >
            Feedback
          </a>
          <Link
            href="/terms"
            className="font-mono text-label text-stone-400 dark:text-stone-600 hover:text-stone-700 dark:hover:text-stone-300 transition-colors tracking-wide"
          >
            Terms
          </Link>
          <Link
            href="/privacy"
            className="font-mono text-label text-stone-400 dark:text-stone-600 hover:text-stone-700 dark:hover:text-stone-300 transition-colors tracking-wide"
          >
            Privacy
          </Link>
          <a
            href="mailto:hello@folioapp.co.uk"
            className="font-mono text-label text-stone-400 dark:text-stone-600 hover:text-stone-700 dark:hover:text-stone-300 transition-colors tracking-wide"
          >
            Contact
          </a>
          <a
            href="https://www.linkedin.com/in/gregory-vitrenko-7258a0350"
            target="_blank"
            rel="noopener noreferrer"
            className="font-mono text-label text-stone-400 dark:text-stone-600 hover:text-stone-700 dark:hover:text-stone-300 transition-colors tracking-wide"
          >
            LinkedIn
          </a>
        </nav>

      </div>
    </footer>
  );
}
```

Key decisions encoded in this markup:
- Feedback and Contact use <a href="mailto:..."> without target="_blank" (mailto links use default browser behaviour)
- LinkedIn uses target="_blank" rel="noopener noreferrer" (external URL, opens new tab)
- All text-[10px] replaced with text-label (same 10px value, named token)
- Link order: Feedback · Terms · Privacy · Contact · LinkedIn (locked decision)
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && grep -c 'LinkedIn\|Feedback' components/SiteFooter.tsx</automated>
  </verify>
  <done>
    - SiteFooter.tsx has exactly 5 links in order: Feedback, Terms, Privacy, Contact, LinkedIn
    - LinkedIn link has target="_blank" and rel="noopener noreferrer"
    - Feedback and Contact use mailto:hello@folioapp.co.uk without target="_blank"
    - All text-[10px] replaced with text-label
    - npx tsc --noEmit passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix sticky-footer layout in app/layout.tsx</name>
  <files>app/layout.tsx</files>
  <action>
Make two targeted changes to app/layout.tsx. Do not touch metadata, viewport, font definitions, ClerkProvider, html element, ScrollToTop, or dynamic export.

CHANGE 1 — Add flex flex-col to body (line 58):
  BEFORE: <body className="bg-stone-50 dark:bg-stone-950 text-stone-900 dark:text-stone-100 font-sans min-h-screen transition-colors duration-200">
  AFTER:  <body className="bg-stone-50 dark:bg-stone-950 text-stone-900 dark:text-stone-100 font-sans min-h-screen flex flex-col transition-colors duration-200">

CHANGE 2 — Wrap {children} in <main className="flex-1"> inside Providers (lines 59-62):
  BEFORE:
    <Providers>
      {children}
      <SiteFooter />
    </Providers>
  AFTER:
    <Providers>
      <main className="flex-1">
        {children}
      </main>
      <SiteFooter />
    </Providers>

DO NOT:
  - Pass className to Providers — it is a client component and does not accept className
  - Wrap Providers itself in a flex-1 container
  - Move ScrollToTop inside Providers
  - Touch ClerkProvider, html element, or font variable classes

Why this works:
  body gets flex flex-col → it becomes a flex container
  main gets flex-1 → it expands to fill available space
  SiteFooter has mt-auto → pinned below the flex-1 main on short pages
  On tall pages, main simply grows with content and footer follows naturally
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - app/layout.tsx body className includes both min-h-screen and flex flex-col
    - {children} is wrapped in <main className="flex-1"> inside Providers
    - SiteFooter remains as a sibling of main inside Providers (not inside main)
    - npx tsc --noEmit passes
    - npm run build succeeds
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. TypeScript compile check:
   cd /Users/gregoryvitrenko/Documents/Folio && npx tsc --noEmit
   Expected: zero errors

2. Build check:
   cd /Users/gregoryvitrenko/Documents/Folio && npm run build
   Expected: build succeeds

3. Confirm five footer links exist:
   grep -n 'Feedback\|LinkedIn\|Terms\|Privacy\|Contact' components/SiteFooter.tsx
   Expected: all five labels present

4. Confirm LinkedIn opens in new tab:
   grep -n 'target="_blank"' components/SiteFooter.tsx
   Expected: one result on the LinkedIn link only

5. Confirm no arbitrary text-[10px] remains in footer:
   grep -n 'text-\[10px\]' components/SiteFooter.tsx
   Expected: zero results

6. Confirm layout structure:
   grep -n 'flex flex-col\|flex-1' app/layout.tsx
   Expected: flex flex-col on body line, flex-1 on main wrapper

Manual verification (commit + push, check live site):
   - Visit a short page (e.g. /terms) — footer should appear at the bottom of the viewport, not mid-page
   - Footer shows: Feedback · Terms · Privacy · Contact · LinkedIn
   - Click LinkedIn — opens new tab
   - Click Feedback — opens email client (no new tab)
</verification>

<success_criteria>
- SiteFooter.tsx renders five links in the correct order with correct href values
- LinkedIn uses target="_blank" rel="noopener noreferrer"
- Feedback/Contact use mailto without target="_blank"
- All text-[10px] in SiteFooter replaced with text-label
- layout.tsx body has flex flex-col; children wrapped in main with flex-1
- npm run build succeeds
- Footer is visually pinned at the bottom on short pages (verified after deploy)
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell/02-02-SUMMARY.md` using the summary template.
</output>
