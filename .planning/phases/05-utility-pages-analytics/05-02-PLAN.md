---
phase: 05-utility-pages-analytics
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/firms/page.tsx
  - app/tests/page.tsx
  - app/primers/page.tsx
autonomous: true
requirements:
  - UTIL-02
  - UTIL-04
  - UTIL-05
must_haves:
  truths:
    - "Firms page count badge uses .section-label + stone background + rounded-chrome (no zinc, no bare rounded)"
    - "Firms page heading icon and h2 text use stone palette and text-subheading font-bold"
    - "Firms disclaimer block uses rounded-card (not rounded-sm)"
    - "Tests page count badge uses rounded-chrome (not bare rounded)"
    - "Tests page body paragraphs use text-caption (not text-[13px])"
    - "Primers page count badge uses rounded-chrome (not bare rounded)"
    - "Primers page body paragraph uses text-caption (not text-[13px])"
  artifacts:
    - path: "app/firms/page.tsx"
      provides: "Firms page — zinc badge replaced with stone + rounded-chrome; disclaimer block uses rounded-card"
      contains: "rounded-chrome"
    - path: "app/tests/page.tsx"
      provides: "Tests page — badge radius fixed to rounded-chrome; arbitrary text-[13px] replaced with text-caption"
      contains: "text-caption"
    - path: "app/primers/page.tsx"
      provides: "Primers page — badge radius fixed to rounded-chrome; arbitrary text-[13px] replaced with text-caption"
      contains: "text-caption"
  key_links:
    - from: "app/firms/page.tsx"
      to: "globals.css .section-label"
      via: "className section-label"
      pattern: "section-label"
    - from: "app/tests/page.tsx"
      to: "tailwind.config.ts text-caption"
      via: "text-caption on p elements"
      pattern: "text-caption"
---

<objective>
Apply the final token fixes to Firms, Tests, and Primers pages. Firms has zinc palette violations in the count badge and heading. Tests and Primers are already stone-compliant but retain two token violations each: a bare `rounded` on the count badge and `text-[13px]` on body paragraphs.

Purpose: Complete the full-sweep of all five utility pages so no page reads as a different product.
Output: Three page files with all token violations resolved.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-utility-pages-analytics/05-RESEARCH.md

<interfaces>
<!-- Established token system — Tailwind v3 (tailwind.config.ts) -->

Token reference:
- text-subheading: 18px / fontWeight 500 → use with explicit font-bold for page headings
- text-caption: 13px / lineHeight 1.5 → replaces text-[13px] everywhere
- rounded-card: 2px → for content containers and info blocks (replaces rounded-sm, rounded-xl)
- rounded-chrome: 4px → for badges and UI chrome (replaces bare "rounded")

Component class (globals.css):
- .section-label: font-mono + text-label (10px) + tracking-widest + uppercase + text-stone-400
  → Use for count badges (add stone background, padding, rounded-chrome separately)
  → Do NOT add text-stone-400 explicitly — it is already encoded in .section-label

Current violations in app/firms/page.tsx (from source audit):
- line 21: text-zinc-400 on Building2 icon → text-stone-400
- line 22: text-lg font-bold text-zinc-900 dark:text-zinc-50 → text-subheading font-bold text-stone-900 dark:text-stone-50 (keep tracking-tight)
- line 25: "font-mono text-[10px] text-zinc-400 dark:text-zinc-500 tracking-widest uppercase bg-zinc-100 dark:bg-zinc-800 px-2 py-0.5 rounded" → "section-label bg-stone-100 dark:bg-stone-800 px-2 py-0.5 rounded-chrome"
- line 31: rounded-sm on disclaimer block → rounded-card

Note: FirmsDirectory.tsx is already stone-compliant — do NOT modify it.

Current violations in app/tests/page.tsx (from source audit):
- line 24: "... px-2 py-0.5 rounded" → change only "rounded" to "rounded-chrome" (the surrounding stone classes are already correct)
- lines 28 and 33: text-[13px] → text-caption

Current violations in app/primers/page.tsx (from source audit):
- line 24: "... px-2 py-0.5 rounded" → change only "rounded" to "rounded-chrome"
- line 28: text-[13px] → text-caption
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate app/firms/page.tsx to design tokens</name>
  <files>app/firms/page.tsx</files>
  <action>
Read app/firms/page.tsx in full, then apply ALL of the following changes:

1. Page heading icon Building2 (around line 21):
   - "text-zinc-400" → "text-stone-400"

2. Page heading h2 (around line 22):
   - "text-lg font-bold text-zinc-900 dark:text-zinc-50" → "text-subheading font-bold text-stone-900 dark:text-stone-50" (keep tracking-tight)

3. Count badge span (around line 25):
   - Replace the entire class string "font-mono text-[10px] text-zinc-400 dark:text-zinc-500 tracking-widest uppercase bg-zinc-100 dark:bg-zinc-800 px-2 py-0.5 rounded" with "section-label bg-stone-100 dark:bg-stone-800 px-2 py-0.5 rounded-chrome"

4. Disclaimer block div (around line 31):
   - "rounded-sm" → "rounded-card" (leave all other classes — bg-amber-50, border, padding — unchanged)

After changes, scan the whole file for any remaining "zinc" classes. Fix any found.
Do NOT touch FirmsDirectory.tsx or any other imported component.
Do NOT change any logic, imports, or structure.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>app/firms/page.tsx: zero zinc violations; count badge uses section-label + rounded-chrome; h2 uses text-subheading font-bold; disclaimer block uses rounded-card; lint passes</done>
</task>

<task type="auto">
  <name>Task 2: Fix token violations in app/tests/page.tsx and app/primers/page.tsx</name>
  <files>app/tests/page.tsx, app/primers/page.tsx</files>
  <action>
These two pages are already on the stone palette. Apply only the two remaining token fixes to each.

For app/tests/page.tsx:
Read the file in full, then:
1. Count badge (around line 24): change "rounded" to "rounded-chrome" within the existing class string (leave all surrounding stone classes unchanged)
2. Body paragraphs (lines 28 and 33): replace "text-[13px]" with "text-caption" (leave all other classes unchanged)

For app/primers/page.tsx:
Read the file in full, then:
1. Count badge (around line 24): change "rounded" to "rounded-chrome" within the existing class string
2. Body paragraph (line 28): replace "text-[13px]" with "text-caption"

Important: Do NOT change any zinc → stone (there are no zinc violations). Do NOT change any logic, imports, or structure. The change is surgical — only the two token types listed above.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>app/tests/page.tsx and app/primers/page.tsx: no text-[13px] remaining; count badges use rounded-chrome; lint passes</done>
</task>

</tasks>

<verification>
After both tasks complete, run: `cd /Users/gregoryvitrenko/Documents/Folio && npm run build 2>&1 | tail -30`

Build must exit 0 with no TypeScript errors.
</verification>

<success_criteria>
- app/firms/page.tsx: zero zinc violations; section-label + rounded-chrome on badge; text-subheading font-bold on heading; rounded-card on disclaimer; lint clean
- app/tests/page.tsx: rounded-chrome on badge; text-caption replacing text-[13px]; lint clean
- app/primers/page.tsx: rounded-chrome on badge; text-caption replacing text-[13px]; lint clean
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-utility-pages-analytics/05-02-SUMMARY.md`
</output>
