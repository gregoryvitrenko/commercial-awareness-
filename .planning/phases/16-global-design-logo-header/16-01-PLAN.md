---
phase: 16-global-design-logo-header
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
autonomous: true
requirements:
  - GDES-01
  - GDES-02

must_haves:
  truths:
    - "Every page has a warm cream background (#F9F7F2) in light mode — not cold white or stone-50"
    - "Dark mode pages retain their existing dark background — warm paper only applies to light mode"
    - "Section/category labels (.section-label) render in JetBrains Mono, visually distinct from Inter body text"
  artifacts:
    - path: "app/globals.css"
      provides: "Updated --paper CSS variable (HSL for #F9F7F2) + .section-label with font-mono"
      contains: "--paper: 40 25% 97%"
    - path: "app/layout.tsx"
      provides: "body uses bg-paper instead of bg-stone-50"
      contains: "bg-paper"
  key_links:
    - from: "app/globals.css"
      to: "tailwind.config.ts"
      via: "--paper CSS variable consumed by paper: 'hsl(var(--paper))' color token"
      pattern: "--paper:"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "bg-paper Tailwind class resolves through --paper variable"
      pattern: "bg-paper"
---

<objective>
Apply the warm paper background and restore JetBrains Mono to section labels — the two global design changes that affect every page in the application.

Purpose: The current cold bg-stone-50 background reads as generic SaaS. #F9F7F2 (warm cream) matches physical newspaper stock and gives the product a premium editorial feel. JetBrains Mono on .section-label provides visual contrast with Inter body text — section labels should feel like metadata stamps, not body copy.

Output: Updated globals.css and layout.tsx. Every page picks up the changes automatically through the shared body class and CSS utility.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key patterns the executor needs. -->

From app/globals.css (current state):
- --paper: 40 20% 98% (this is near-white — target is #F9F7F2 which is approximately HSL(40, 25%, 97%))
- .section-label { @apply font-sans text-label tracking-widest uppercase text-stone-400 dark:text-stone-500; }
  → change font-sans to font-mono

From tailwind.config.ts:
- paper: 'hsl(var(--paper))' — already wired, just need to update the --paper value

From app/layout.tsx line 79:
- body className: "bg-stone-50 dark:bg-stone-950 text-stone-900 dark:text-stone-100 font-sans min-h-screen flex flex-col transition-colors duration-200"
  → change bg-stone-50 to bg-paper; dark:bg-stone-950 stays (dark mode already correct)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update --paper CSS variable to warm #F9F7F2 and restore font-mono to .section-label</name>
  <files>app/globals.css</files>
  <action>
    Two changes to globals.css:

    1. In the :root block, change --paper from `40 20% 98%` to `40 25% 97%`.
       This makes bg-paper render as #F9F7F2 (warm cream). The HSL values: Hue 40 (warm yellow-orange), Saturation 25%, Lightness 97%.
       Dark mode --paper in .dark block stays as-is (`20 10% 6%` — warm dark).

    2. In .section-label, change `font-sans` to `font-mono`.
       Before: @apply font-sans text-label tracking-widest uppercase text-stone-400 dark:text-stone-500;
       After:  @apply font-mono text-label tracking-widest uppercase text-stone-400 dark:text-stone-500;

    Do NOT change anything else in globals.css — the print styles, selection colour, and other utilities must remain intact.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Confirm no TypeScript errors.
    Visually: bg-paper should resolve to a warm cream, not cold white. .section-label should pick up JetBrains Mono (the --font-mono variable is already registered in layout.tsx).
  </verify>
  <done>--paper HSL updated, .section-label uses font-mono, no TS errors</done>
</task>

<task type="auto">
  <name>Task 2: Switch body background from bg-stone-50 to bg-paper in layout.tsx</name>
  <files>app/layout.tsx</files>
  <action>
    In app/layout.tsx, find the body element className (line 79):
    "bg-stone-50 dark:bg-stone-950 text-stone-900 dark:text-stone-100 font-sans min-h-screen flex flex-col transition-colors duration-200"

    Change bg-stone-50 to bg-paper. Keep dark:bg-stone-950 — dark mode does not use the paper variable.
    Result: "bg-paper dark:bg-stone-950 text-stone-900 dark:text-stone-100 font-sans min-h-screen flex flex-col transition-colors duration-200"

    Do NOT change any other part of layout.tsx — imports, metadata, fonts, JSON-LD, providers all stay intact.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Run: npx next build (or ask user to check dev server)
    Light mode body should show warm cream background. Dark mode should be unchanged (dark stone-950).
  </verify>
  <done>body uses bg-paper, dark:bg-stone-950 preserved, build passes</done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit — zero errors
2. In browser light mode: page background is visibly warm cream (not cold white) on home, article, firm, quiz, archive pages
3. In browser dark mode: background is unchanged dark stone-950
4. Any .section-label element (topic tabs, mobile nav section headers, etc.) renders in JetBrains Mono — monospace letterforms clearly distinct from Inter body text
</verification>

<success_criteria>
- body background in light mode: #F9F7F2 warm cream (no cold white remnants)
- body background in dark mode: unchanged stone-950
- .section-label: font-mono (JetBrains Mono) — visually distinct from body text
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-global-design-logo-header/16-01-SUMMARY.md`
</output>
