---
phase: 18-secondary-pages-conversion-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/FirmsDirectory.tsx
  - app/upgrade/page.tsx
autonomous: true
requirements:
  - FDIR-01
  - CONV-04
  - CONV-05

must_haves:
  truths:
    - "Tier section headers (Magic Circle, Silver Circle, etc.) in the firms directory use a visible horizontal rule above the tier name — not just bold text"
    - "Upgrade page outcome grid item titles (6 items) render in the project serif font (Playfair Display) — visually distinct from the body Inter text below each title"
    - "The upgrade page CTA area ends the page with a dark stone-900 panel — no amber button visible"
    - "The dark CTA panel contains a border-style button (not filled amber) for the checkout action"
  artifacts:
    - path: "components/FirmsDirectory.tsx"
      provides: "Tier section header with editorial rule treatment"
      contains: "border-t border-stone-"
    - path: "app/upgrade/page.tsx"
      provides: "Serif outcome titles, dark CTA panel"
      contains: "font-serif"
      contains_also: "bg-stone-900"
  key_links:
    - from: "app/upgrade/page.tsx OUTCOMES array"
      to: "outcome label rendering"
      via: "outcome.label mapped to p element — must add font-serif"
      pattern: "outcome\\.label"
    - from: "app/upgrade/page.tsx CTA button"
      to: "handleUpgrade()"
      via: "dark panel wraps button — same onClick, different visual container"
      pattern: "handleUpgrade"
---

<objective>
Apply editorial tier rule treatment to the firms directory, add serif font to upgrade page outcome titles, and replace the amber CTA button with a dark stone-900 conversion panel.

Purpose: Firms directory tiers become scannable newspaper-section separators. Upgrade page achieves editorial consistency (serif titles) and authoritative visual anchor (dark CTA panel).
Output: FirmsDirectory.tsx with rule headers; upgrade/page.tsx with serif titles and dark panel.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components/FirmsDirectory.tsx
@app/upgrade/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From components/FirmsDirectory.tsx (tier header, lines ~105-113):
```tsx
// Current: just bold sans text, no rule
<h3 className="font-sans text-sm tracking-widest uppercase text-stone-900 dark:text-stone-100 mb-3">
  {tier}
  {q && <span className="ml-2 font-sans normal-case tracking-normal text-stone-400 dark:text-stone-500">({tierFirms.length})</span>}
</h3>
```

From app/upgrade/page.tsx (outcome grid item, lines ~104-117):
```tsx
// Current: text-caption font-semibold — NOT serif
<div key={outcome.label} className="bg-white dark:bg-stone-900 px-5 py-4">
  <p className="text-caption font-semibold text-stone-900 dark:text-stone-100 mb-1">
    {outcome.label}
  </p>
  <p className="text-caption text-stone-500 dark:text-stone-400 leading-relaxed">
    {outcome.body}
  </p>
</div>
```

From app/upgrade/page.tsx (CTA area, lines ~119-152):
```tsx
// Current: white card with amber bg-amber-600 button
<div className="rounded-card bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 px-6 py-6">
  ...
  <button className="w-full py-3 rounded-chrome bg-amber-600 text-stone-50 ...">
    Subscribe — £4/month →
  </button>
```

Design tokens (must use):
- `font-serif` — Playfair Display, applies to headings/titles
- `rounded-card` (2px) for cards/panels
- `rounded-chrome` (4px) for buttons/chips
- `.section-label` for overline labels
- No `rounded-xl` or other non-standard radii
- No emojis in output text
- Stone palette: `bg-stone-900`, `text-stone-50`, `border-stone-700`, `text-stone-200`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editorial rule treatment for tier headers in FirmsDirectory</name>
  <files>components/FirmsDirectory.tsx</files>
  <action>
Replace the current plain tier header `<h3>` with an editorial newspaper-section header pattern: a full-width horizontal rule above the tier name, with the tier name in the existing tracking-widest uppercase style.

Replace the existing `<h3>` at around line 106 with:

```tsx
<div className="mb-4">
  {/* Full-width editorial rule above tier name */}
  <div className="h-px bg-stone-900 dark:bg-stone-100 mb-3" />
  <h3 className="flex items-baseline gap-3 font-sans text-sm tracking-widest uppercase text-stone-900 dark:text-stone-100">
    {tier}
    {q && (
      <span className="font-sans normal-case tracking-normal text-label text-stone-400 dark:text-stone-500">
        {tierFirms.length} firms
      </span>
    )}
  </h3>
</div>
```

The outer container div already has `scroll-mt-28` and `id={tierId(tier)}` — keep those attributes unchanged. Only replace the `<h3>` element with the new `<div>` block above. The surrounding `<div key={tier} id={...} className="scroll-mt-28">` wrapper stays the same.

No changes to: search bar, jump links, firm cards, empty state, search count.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Each tier section (Magic Circle, Silver Circle, etc.) shows a full-width dark horizontal rule above the tier name. Rule is full width of the content column. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Serif outcome titles and dark stone-900 CTA panel on upgrade page</name>
  <files>app/upgrade/page.tsx</files>
  <action>
Two targeted changes to app/upgrade/page.tsx:

**Change 1 — Serif outcome titles:**
In the outcomes grid (around line 109), change the label `<p>` class from `text-caption font-semibold` to `font-serif text-[15px] font-semibold`. Playfair Display at ~15px looks better than text-caption (12px) for a title. Keep all other classes the same:

```tsx
// Before:
<p className="text-caption font-semibold text-stone-900 dark:text-stone-100 mb-1">

// After:
<p className="font-serif text-[15px] font-semibold text-stone-900 dark:text-stone-100 mb-1 leading-snug">
```

**Change 2 — Dark stone-900 CTA panel (replacing pricing card + amber button):**
The pricing card area (the right column on desktop, lines ~120-163) currently shows a white card with an amber button and a free tier note below. Replace this entirely with a dark editorial conversion panel:

```tsx
{/* Dark conversion panel */}
<div className="flex flex-col gap-4">
  <div className="rounded-card bg-stone-900 dark:bg-stone-950 px-6 py-7">
    {/* Overline */}
    <p className="section-label text-stone-400 mb-3">Monthly subscription</p>

    {/* Price */}
    <p className="font-serif text-display font-bold tracking-tight text-stone-50 leading-none mb-1">
      £4
    </p>
    <p className="text-caption text-stone-500 mb-6">
      per month · cancel any time
    </p>

    {/* Feature list — 3 bullet points in mono style */}
    <ul className="space-y-2 mb-6">
      {['Full article access + talking points', 'Daily quiz + firm interview packs', 'Audio briefing + full archive'].map((item) => (
        <li key={item} className="flex items-start gap-2.5">
          <span className="mt-[3px] w-1 h-1 rounded-full bg-stone-500 flex-shrink-0" />
          <span className="text-caption text-stone-400 leading-relaxed">{item}</span>
        </li>
      ))}
    </ul>

    {error && (
      <p className="text-label font-sans text-rose-400 mb-3">{error}</p>
    )}

    {/* Border-style CTA button — authoritative but not amber */}
    <button
      onClick={handleUpgrade}
      disabled={loading}
      className="w-full py-3 rounded-chrome border border-stone-600 text-stone-200 text-caption font-sans font-medium hover:border-stone-400 hover:text-stone-50 transition-colors duration-200 disabled:opacity-50 disabled:cursor-not-allowed"
    >
      {loading
        ? 'Redirecting to checkout…'
        : isSignedIn
        ? 'Subscribe — £4/month →'
        : 'Create account & subscribe →'}
    </button>

    <p className="text-center text-label text-stone-600 mt-3">
      Billed via Stripe · cancel any time
    </p>
  </div>

  {/* Free tier note — keep below dark panel */}
  <div className="rounded-card bg-stone-50 dark:bg-stone-800/40 border border-stone-200 dark:border-stone-700 px-5 py-4">
    <p className="text-label font-sans font-semibold text-stone-700 dark:text-stone-300 mb-1 uppercase tracking-widest">
      Free tier
    </p>
    <p className="text-caption text-stone-500 dark:text-stone-400 leading-relaxed">
      Daily briefing headlines and summaries are always free. No credit card required to browse.
    </p>
  </div>
</div>
```

Also update the main `<div>` background from `bg-stone-50 dark:bg-stone-950` to `bg-paper dark:bg-stone-950` to match the sitewide paper background established in Phase 16.

Do NOT change: the `OUTCOMES` array, the pull quote section, the editorial headline section, the page header, the `handleUpgrade` function, or the Vercel analytics tracking calls.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Outcome grid titles render in Playfair Display serif font (visibly different from body text). CTA panel is dark stone-900 with a border-style button. No amber visible on the page. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx next build` completes (or no new errors introduced)
- Firms directory: each tier section has a full-width dark rule line above the tier name
- Upgrade page: outcome labels are in serif font, larger than the body text below
- Upgrade page: CTA area is a dark stone-900 panel with border button — amber CTA is gone
</verification>

<success_criteria>
1. Firms directory tier headers use full-width horizontal rule (dark line above tier name)
2. Upgrade page outcome titles use font-serif (Playfair Display) — visually distinct from caption body
3. Upgrade page CTA panel is bg-stone-900 with border-style button (border-stone-600 text-stone-200)
4. No amber button visible anywhere on the upgrade page
5. TypeScript compiles clean with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/18-secondary-pages-conversion-polish/18-02-SUMMARY.md`
</output>
