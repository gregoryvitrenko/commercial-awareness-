---
phase: 20-design-system-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - tailwind.config.ts
autonomous: true
requirements:
  - ROUND-01
  - ROUND-02
  - COL-01

must_haves:
  truths:
    - "Cards and panels across all pages have visibly rounded corners (24px) — the sharp editorial aesthetic is replaced by a softer premium look"
    - "Chrome elements (buttons, badges) use 16px radius — softened from 4px but not fully pill-shaped via token"
    - "The oxford-blue colour (#002147) is available as a Tailwind utility class (bg-oxford-blue, text-oxford-blue, border-oxford-blue)"
  artifacts:
    - path: "app/globals.css"
      provides: "Updated --radius-card (1.5rem / 24px) and --radius-chrome (1rem / 16px) CSS variables"
      contains: "--radius-card:   1.5rem"
    - path: "tailwind.config.ts"
      provides: "oxford-blue colour token registered in theme.extend.colors"
      contains: "oxford-blue"
  key_links:
    - from: "app/globals.css"
      to: "tailwind.config.ts"
      via: "CSS variable --radius-card referenced by borderRadius.card"
      pattern: "var\\(--radius-card\\)"
    - from: "tailwind.config.ts"
      to: "all components using rounded-card"
      via: "Tailwind JIT class generation from CSS variable value"
      pattern: "rounded-card"
---

<objective>
Update the two design system token files to establish the v2 rounded aesthetic and Oxford blue colour. No component edits — token changes propagate automatically to all 17 files using `rounded-card` and `rounded-chrome`.

Purpose: Establish the token foundation for the v2 Premium Experience aesthetic shift — softer radius and Oxford blue accent — before any component-level colour replacements.
Output: globals.css with updated radius variables; tailwind.config.ts with oxford-blue colour registration.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update radius CSS variables in globals.css</name>
  <files>app/globals.css</files>
  <action>
In the `:root` block, update the two semantic radius tokens:

Change `--radius-card` from `0.125rem` to `1.5rem` (24px — rounded-3xl equivalent):
```
--radius-card:   1.5rem;  /* 24px — rounded-3xl, premium soft cards */
```

Change `--radius-chrome` from `0.25rem` to `1rem` (16px — rounded-2xl equivalent):
```
--radius-chrome: 1rem;    /* 16px — rounded-2xl, softened chrome */
```

Leave `--radius-pill` (9999px) and `--radius-input` (0.25rem) unchanged.

Also update the inline comment on --radius-card from "nearly flat, editorial" to "24px — rounded-3xl, premium soft cards".
Update the inline comment on --radius-chrome from "badges, UI chrome elements" to "16px — rounded-2xl, softened chrome".

Do NOT touch any other variables, dark mode block, or @layer definitions.
  </action>
  <verify>
    <automated>grep -n "radius-card\|radius-chrome" /Users/gregoryvitrenko/Documents/Folio/app/globals.css</automated>
  </verify>
  <done>globals.css shows `--radius-card: 1.5rem` and `--radius-chrome: 1rem` in the :root block.</done>
</task>

<task type="auto">
  <name>Task 2: Register oxford-blue colour in tailwind.config.ts</name>
  <files>tailwind.config.ts</files>
  <action>
In `theme.extend.colors`, add `oxford-blue` as a flat colour token with light and dark variants:

```typescript
'oxford-blue': {
  DEFAULT: '#002147',
  light: '#1a3a5c',   /* slightly lighter for hover states */
  dark:  '#001530',   /* slightly deeper for dark mode */
},
```

Place this entry inside the existing `colors: { ... }` block in `theme.extend`, after the `paper:` entry (last item before the closing brace).

Do NOT touch the safelist block — amber topic colours there (bg-amber-700, text-amber-800, etc.) are Regulation topic colours used in TOPIC_STYLES and must remain safelisted.

Do NOT remove any existing colour definitions.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project /Users/gregoryvitrenko/Documents/Folio/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles without errors. `oxford-blue` appears in tailwind.config.ts colors block with DEFAULT, light, and dark variants.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "radius-card" app/globals.css` shows `1.5rem`
2. `grep "radius-chrome" app/globals.css` shows `1rem`
3. `grep "oxford-blue" tailwind.config.ts` returns the colour definition
4. `npx tsc --noEmit` passes with no errors
</verification>

<success_criteria>
- `--radius-card` is 1.5rem (24px) in globals.css
- `--radius-chrome` is 1rem (16px) in globals.css
- `oxford-blue` colour (DEFAULT: #002147) is registered in tailwind.config.ts
- TypeScript compiles clean
- No other tokens, safelist entries, or component files are touched
</success_criteria>

<output>
After completion, create `.planning/phases/20-design-system-tokens/20-01-SUMMARY.md`
</output>
