---
phase: 20-design-system-tokens
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - components/BookmarkButton.tsx
  - components/QuizInterface.tsx
autonomous: true
requirements:
  - COL-01

must_haves:
  truths:
    - "The bookmark active state (saved/bookmarked) renders in Oxford blue — not amber"
    - "The quiz high-score trophy icon renders in Oxford blue — not amber"
    - "The quiz streak label accent renders in Oxford blue — not amber"
    - "Amber topic colour usages (Regulation topic, US Firms tier, warning panels, urgency indicators) are untouched"
  artifacts:
    - path: "components/BookmarkButton.tsx"
      provides: "Oxford blue bookmark active state — both card and article variants"
      contains: "oxford-blue"
    - path: "components/QuizInterface.tsx"
      provides: "Oxford blue trophy icon and streak label accent"
      contains: "oxford-blue"
  key_links:
    - from: "components/BookmarkButton.tsx"
      to: "tailwind.config.ts"
      via: "bg-oxford-blue / text-oxford-blue / border-oxford-blue classes"
      pattern: "oxford-blue"
    - from: "components/QuizInterface.tsx"
      to: "tailwind.config.ts"
      via: "text-oxford-blue class"
      pattern: "oxford-blue"
---

<objective>
Replace amber CTA/active-state colour usages with Oxford blue in the two components that use amber as an accent for user actions and highlights. Amber topic colours, urgency indicators, and semantic decorative uses are preserved.

Purpose: Complete COL-01 by making Oxford blue the visible primary action colour — bookmark active state and quiz score accent are the most user-visible amber-as-action instances.
Output: BookmarkButton and QuizInterface with oxford-blue accent classes replacing amber in active/CTA roles.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claire/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-design-system-tokens/20-01-SUMMARY.md
</context>

<interfaces>
<!-- oxford-blue colour registered in tailwind.config.ts by Plan 01 -->
<!-- Available Tailwind classes: bg-oxford-blue, bg-oxford-blue/80, text-oxford-blue, border-oxford-blue -->
<!-- Also available: bg-oxford-blue-light (hover), bg-oxford-blue-dark (dark mode) -->
<!-- #002147 — very dark navy. On dark backgrounds use text-stone-50 for contrast. -->
<!-- On light backgrounds: text-oxford-blue is legible (dark navy on cream). -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace amber bookmark active state with oxford-blue</name>
  <files>components/BookmarkButton.tsx</files>
  <action>
In the `article` variant button (lines ~44-46), replace the saved-state amber classes:

Current:
```
'border-amber-300 dark:border-amber-600 bg-amber-50 dark:bg-amber-900/20 text-amber-700 dark:text-amber-400'
```

Replace with:
```
'border-oxford-blue/30 dark:border-oxford-blue/50 bg-oxford-blue/5 dark:bg-oxford-blue/10 text-oxford-blue dark:text-oxford-blue-light'
```

In the `card` variant button (lines ~61), replace the saved-state amber text:

Current:
```
'text-amber-500 dark:text-amber-400'
```

Replace with:
```
'text-oxford-blue dark:text-oxford-blue-light'
```

No other changes. Do NOT touch the border-stone / text-stone unsaved state classes.

Note: `rounded-sm` on line 43 of the article variant — leave as-is. This is a deliberate small radius for an inline button. ROUND-02 only applies to chrome elements using `rounded-chrome`; this uses `rounded-sm` by specific design choice.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project /Users/gregoryvitrenko/Documents/Folio/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>BookmarkButton.tsx uses oxford-blue classes for the saved/active state in both card and article variants. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace amber accent in QuizInterface trophy and streak label</name>
  <files>components/QuizInterface.tsx</files>
  <action>
Make the following three targeted replacements in QuizInterface.tsx:

**1. Progress bar mid-range colour (line ~498):**
Current:
```
pct >= 60 ? 'bg-amber-500 dark:bg-amber-400' :
```
Replace with:
```
pct >= 60 ? 'bg-oxford-blue/70 dark:bg-oxford-blue-light' :
```

**2. Streak label accent (line ~578):**
Current:
```
<p className="text-label font-sans font-semibold tracking-[0.14em] uppercase text-amber-400">
```
Replace with:
```
<p className="text-label font-sans font-semibold tracking-[0.14em] uppercase text-oxford-blue dark:text-oxford-blue-light">
```

**3. Trophy icon high-score colour (line ~756):**
Current:
```
${pct >= 80 ? 'text-amber-500' : pct >= 60 ? 'text-stone-400' : 'text-stone-300 dark:text-stone-600'}
```
Replace with:
```
${pct >= 80 ? 'text-oxford-blue dark:text-oxford-blue-light' : pct >= 60 ? 'text-stone-400' : 'text-stone-300 dark:text-stone-600'}
```

**Do NOT touch:**
- `bg-amber-500 dark:bg-amber-400` on FirmQuiz component dot (different file, not in scope)
- Any amber colour used in topic-keyed colour maps (these are named `amber` as a topic key, not an action colour)
- `bg-amber-500` progress bar for pct < 60 condition — wait, there is no amber below 60; amber is the 60-79 band. The above replacement handles it.

Use exact line-by-line search-and-replace. Do not rewrite surrounding code.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project /Users/gregoryvitrenko/Documents/Folio/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>QuizInterface.tsx uses oxford-blue for progress bar mid-range, streak label, and high-score trophy. TypeScript compiles clean. grep for text-amber in QuizInterface returns only lines inside topic-keyed colour maps (if any).</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "amber" components/BookmarkButton.tsx` returns no results
2. `grep "text-amber-400\|text-amber-500\|bg-amber-500" components/QuizInterface.tsx` returns 0 action-context results (only topic-map entries if present)
3. `grep "oxford-blue" components/BookmarkButton.tsx components/QuizInterface.tsx` returns the replaced lines
4. `npx tsc --noEmit` passes with no errors

**Amber usages intentionally preserved (do NOT replace):**
- `components/SectorWatch.tsx` — pulsing amber dot = "live story" semantic signal
- `components/TrackerDashboard.tsx` — amber urgency colours (30-day deadline warning = status indicator)
- `components/InterviewPractice.tsx` / `components/AreaQuiz.tsx` — amber key in topic colour maps (Regulation topic = amber by design)
- `app/firms/[slug]/page.tsx` — US Firms tier colour + AlertTriangle warning panel
- `components/SavedView.tsx` — amber note border (editorial decoration)
- `components/PrimerView.tsx` — amber blockquote border (editorial decoration)
- `components/CommentsSection.tsx` — amber star/rating indicator
- `components/FirmQuiz.tsx` — amber dot for quiz answer indicator
- `app/interview/page.tsx` — amber key in topic colour config
- `app/tests/[slug]/page.tsx` — amber badge for score state
</verification>

<success_criteria>
- BookmarkButton active/saved state renders in oxford-blue (dark navy) — visible change from amber
- QuizInterface streak label and 80%+ trophy render in oxford-blue
- QuizInterface 60-80% progress bar renders in oxford-blue (muted opacity)
- All amber topic/tier/urgency/decoration usages unchanged
- TypeScript compiles clean with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-design-system-tokens/20-02-SUMMARY.md`
</output>
