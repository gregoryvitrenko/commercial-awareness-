---
phase: 22-secondary-page-redesigns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/quiz/page.tsx
  - components/PrimerCard.tsx
autonomous: true
requirements: [QUIZ-01, PRIM-01]

must_haves:
  truths:
    - "The quiz page opens with a prominent 'Today' hero card as its visual focal point — before any date list"
    - "The hero card shows today's date in large font, question count, and an Oxford blue start CTA button"
    - "The primers page card grid has icons matching each topic's colour from TOPIC_STYLES"
    - "Each primer card shows a topic icon, title, strapline (2 lines max), and a stat footer"
    - "Primer cards use rounded-card (24px from Phase 20) corners"
  artifacts:
    - path: "app/quiz/page.tsx"
      provides: "Quiz page with hero card + date archive list"
      contains: "bg-\\[#002147\\]"
    - path: "components/PrimerCard.tsx"
      provides: "Primer card with topic icon, rounded-card styling"
      contains: "rounded-card"
  key_links:
    - from: "app/quiz/page.tsx"
      to: "/quiz (today's quiz)"
      via: "Oxford blue CTA Link href='/quiz'"
      pattern: "bg-\\[#002147\\]"
    - from: "components/PrimerCard.tsx"
      to: "TOPIC_STYLES"
      via: "icon color class lookup"
      pattern: "TOPIC_STYLES"
---

<objective>
Add a prominent "Today" hero card to the quiz page that anchors the entry experience with date, question count, and an Oxford blue start CTA. Simultaneously, update PrimerCard to use rounded-card corners and a topic icon that matches the topic colour — replacing the small dot indicator.

Purpose: Both changes replace the generic flat-card-stack pattern on their respective pages with a layout treatment suited to the page's purpose — the quiz page gets a focal point hero, the primers page gets a richer card that lets users assess topic coverage at a glance.

Output: Updated app/quiz/page.tsx, updated components/PrimerCard.tsx
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/types.ts — TopicCategory and TOPIC_STYLES:
```typescript
export type TopicCategory =
  | 'M&A'
  | 'Capital Markets'
  | 'Banking & Finance'
  | 'Energy & Tech'
  | 'Regulation'
  | 'Disputes'
  | 'International'
  | 'AI & Law';

export const TOPIC_STYLES: Record<TopicCategory, { label: string; dot: string }> = {
  'M&A':              { label: 'text-blue-800 dark:text-blue-300',    dot: 'bg-blue-700 dark:bg-blue-400' },
  'Capital Markets':  { label: 'text-violet-800 dark:text-violet-300', dot: 'bg-violet-700 dark:bg-violet-400' },
  'Banking & Finance':{ label: 'text-orange-800 dark:text-orange-300', dot: 'bg-orange-700 dark:bg-orange-400' },
  'Energy & Tech':    { label: 'text-emerald-800 dark:text-emerald-300', dot: 'bg-emerald-700 dark:bg-emerald-400' },
  'Regulation':       { label: 'text-amber-800 dark:text-amber-300',  dot: 'bg-amber-700 dark:bg-amber-400' },
  'Disputes':         { label: 'text-rose-800 dark:text-rose-300',    dot: 'bg-rose-700 dark:bg-rose-400' },
  'International':    { label: 'text-teal-800 dark:text-teal-300',    dot: 'bg-teal-700 dark:bg-teal-400' },
  'AI & Law':         { label: 'text-indigo-800 dark:text-indigo-300', dot: 'bg-indigo-700 dark:bg-indigo-400' },
};
```

From lib/types.ts — Primer interface:
```typescript
export interface Primer {
  slug: string;
  title: string;
  category: TopicCategory;
  strapline: string;
  readTimeMinutes: number;
  sections: { heading: string; body: string }[];
  keyTerms: string[];
  interviewQuestions?: string[];
}
```

From tailwind.config.ts — radius tokens:
```
rounded-card   → var(--radius-card)    currently 2px, Phase 20 will set to 24px
rounded-chrome → var(--radius-chrome)  currently 4px
```

Oxford blue accent: #002147 (use as Tailwind arbitrary bg-[#002147])

TOPIC icon mapping for PrimerCard (lucide-react, size 20):
- 'M&A' → TrendingUp (blue)
- 'Capital Markets' → BarChart2 (violet)
- 'Banking & Finance' → Landmark (orange)
- 'Energy & Tech' → Zap (emerald)
- 'Regulation' → Scale (amber)
- 'Disputes' → Gavel (rose)
- 'International' → Globe (teal)
- 'AI & Law' → Bot (indigo)

Icon color classes derived from TOPIC_STYLES dot color:
- blue → text-blue-700 dark:text-blue-400
- violet → text-violet-700 dark:text-violet-400
- orange → text-orange-700 dark:text-orange-400
- emerald → text-emerald-700 dark:text-emerald-400
- amber → text-amber-700 dark:text-amber-400
- rose → text-rose-700 dark:text-rose-400
- teal → text-teal-700 dark:text-teal-400
- indigo → text-indigo-700 dark:text-indigo-400
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quiz page — add "Today" hero card above the date archive list</name>
  <files>app/quiz/page.tsx</files>
  <action>
In app/quiz/page.tsx, add a prominent hero card between the page heading and the dateList section. The hero card sits ABOVE the date archive list — it is the visual focal point of the page.

Hero card structure:
- Outer: `<div className="rounded-card bg-[#002147] p-6 sm:p-8 mb-8 flex flex-col sm:flex-row sm:items-end sm:justify-between gap-6">` (Oxford blue background, rounded-card, generous padding)
- Left column:
  - Small overline label: `<p className="section-label text-stone-300 mb-2">Today's Quiz</p>`
  - Date in large serif: `<p className="font-serif text-3xl sm:text-4xl font-bold text-white leading-tight">{formatDisplayDate(today)}</p>` — use the existing `formatDisplayDate` helper with `today`
  - Beneath the date: `<p className="text-sm text-stone-300 mt-2">{storyCount} daily · {deepCount} deep practice</p>`
- Right column (CTA):
  - Link to `/quiz` (which loads today's quiz): `<Link href="/quiz" className="inline-flex items-center gap-2 bg-white text-[#002147] font-semibold text-sm px-5 py-3 rounded-chrome hover:bg-stone-100 transition-colors">Start today's quiz <ChevronRight size={14} /></Link>`

The hero card is placed AFTER the page `<h2>` heading block and BEFORE the `{dateList}` render — both in the "no briefing" fallback branch and in the main return. In the "no briefing" fallback, render the hero without the question count line (briefing unavailable — omit the storyCount/deepCount line).

Imports: `ChevronRight` is already imported. No new imports needed.

Do NOT touch the QuizInterface component, the dateList JSX, or any of the server-side data fetching logic.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project /Users/gregoryvitrenko/Documents/Folio/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>app/quiz/page.tsx has a rounded-card bg-[#002147] hero block with today's date in font-serif, question count, and a white CTA button — renders above the date archive list. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: PrimerCard — add topic icon, update to rounded-card, use section-label utility</name>
  <files>components/PrimerCard.tsx</files>
  <action>
Rewrite PrimerCard.tsx to:

1. Replace `rounded-sm` with `rounded-card` on the article element.
2. Replace the small dot + category text row with a larger topic icon (size 20 from lucide-react) in the topic colour.
3. Use the `.section-label` CSS utility class for the category label (not raw inline styles).
4. The card layout becomes: icon row (top) → title (font-serif, large) → strapline (2 lines, line-clamp-2) → footer stat row.

Icon map — create a const inside the file:
```typescript
const TOPIC_ICONS: Record<TopicCategory, React.ElementType> = {
  'M&A':              TrendingUp,
  'Capital Markets':  BarChart2,
  'Banking & Finance': Landmark,
  'Energy & Tech':    Zap,
  'Regulation':       Scale,
  'Disputes':         Gavel,
  'International':    Globe,
  'AI & Law':         Bot,
};
```

Icon color map (text color matching the topic dot color):
```typescript
const TOPIC_ICON_COLORS: Record<TopicCategory, string> = {
  'M&A':              'text-blue-700 dark:text-blue-400',
  'Capital Markets':  'text-violet-700 dark:text-violet-400',
  'Banking & Finance':'text-orange-700 dark:text-orange-400',
  'Energy & Tech':    'text-emerald-700 dark:text-emerald-400',
  'Regulation':       'text-amber-700 dark:text-amber-400',
  'Disputes':         'text-rose-700 dark:text-rose-400',
  'International':    'text-teal-700 dark:text-teal-400',
  'AI & Law':         'text-indigo-700 dark:text-indigo-400',
};
```

New card structure:
```tsx
<Link href={`/primers/${primer.slug}`} className="block group">
  <article className="flex flex-col h-full bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 rounded-card px-5 pt-5 pb-5 hover:bg-stone-50 dark:hover:bg-stone-800/40 transition-colors">
    {/* Icon + category row */}
    <div className="flex items-center gap-2 mb-4">
      <Icon size={20} className={iconColor} />
      <span className={`section-label ${styles.label}`}>{primer.category}</span>
    </div>

    {/* Title */}
    <h3 className="font-serif text-[18px] font-bold leading-snug text-stone-900 dark:text-stone-50 tracking-tight group-hover:underline decoration-stone-400 dark:decoration-stone-500 underline-offset-2 mb-2">
      {primer.title}
    </h3>

    {/* Strapline — capped at 2 lines */}
    <p className="text-caption text-stone-500 dark:text-stone-400 leading-relaxed mb-4 line-clamp-2">
      {primer.strapline}
    </p>

    {/* Footer */}
    <div className="mt-auto pt-3 border-t border-stone-100 dark:border-stone-800 flex items-center justify-between">
      <div className="flex items-center gap-4">
        <span className="text-[11px] text-stone-400 dark:text-stone-500">
          <span className="font-sans font-semibold text-stone-600 dark:text-stone-300">{primer.sections.length}</span>{' '}sections
        </span>
        {primer.interviewQuestions && primer.interviewQuestions.length > 0 && (
          <span className="text-[11px] text-stone-400 dark:text-stone-500">
            <span className="font-sans font-semibold text-stone-600 dark:text-stone-300">{primer.interviewQuestions.length}</span>{' '}interview Qs
          </span>
        )}
      </div>
      <ChevronRight size={14} className="shrink-0 text-stone-300 dark:text-stone-600 group-hover:text-stone-500 dark:group-hover:text-stone-400 transition-colors" />
    </div>
  </article>
</Link>
```

Required imports: `TrendingUp, BarChart2, Landmark, Zap, Scale, Gavel, Globe, Bot, ChevronRight` from lucide-react. Remove `Clock` import (no longer used). Keep `BookOpen` only if used — otherwise remove.

The `TopicCategory` import is already present via `@/lib/types`. Add `React` import for `React.ElementType`.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project /Users/gregoryvitrenko/Documents/Folio/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>PrimerCard.tsx uses rounded-card, shows a topic icon (size 20) in topic colour, strapline is line-clamp-2, footer shows interview question count when available. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
After both tasks, run:
```
npx tsc --noEmit --project /Users/gregoryvitrenko/Documents/Folio/tsconfig.json 2>&1 | head -20
```
Zero TypeScript errors expected.

Manual check (localhost:3001 after user runs dev server):
- /quiz page: Oxford blue hero card visible at top, today's date in large serif, white CTA button
- /primers page: each card has a topic icon (not just a dot), strapline is 2 lines max
</verification>

<success_criteria>
1. Quiz page has a prominent bg-[#002147] hero card at the top showing today's date in font-serif text-3xl+ and a white rounded-chrome CTA button
2. PrimerCard uses rounded-card (not rounded-sm), shows a lucide topic icon in topic colour instead of the dot
3. TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-secondary-page-redesigns/22-01-SUMMARY.md`
</output>
