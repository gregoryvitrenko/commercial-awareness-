---
phase: 25-accent-global-headings
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - app/archive/page.tsx
  - app/quiz/page.tsx
  - app/firms/page.tsx
  - app/tests/page.tsx
  - app/primers/page.tsx
  - app/events/page.tsx
  - app/saved/page.tsx
  - app/interview/page.tsx
  - app/podcast/page.tsx
autonomous: true
requirements:
  - DESIGN-02

must_haves:
  truths:
    - "Every primary content page opens with a recognisable heading block: small spaced overline, large serif title, optional description"
    - "The overline uses tracking-[0.3em] — noticeably wider than the section-label tracking-widest"
    - "The serif title is large (text-5xl) — a tangible visual step up from the old text-subheading headings"
    - "The heading pattern is visually consistent across all 9 pages in both light and dark modes"
  artifacts:
    - path: "app/archive/page.tsx"
      provides: "heading block with overline + text-5xl serif title"
    - path: "app/quiz/page.tsx"
      provides: "heading block replacing old icon+title row"
    - path: "app/firms/page.tsx"
      provides: "heading block replacing old icon+title row"
    - path: "app/tests/page.tsx"
      provides: "heading block replacing old icon+title row"
    - path: "app/primers/page.tsx"
      provides: "heading block replacing old icon+title row"
    - path: "app/events/page.tsx"
      provides: "heading block replacing old icon+title row"
    - path: "app/saved/page.tsx"
      provides: "heading block replacing old icon+title row"
    - path: "app/interview/page.tsx"
      provides: "heading block replacing old icon+title row"
    - path: "app/podcast/page.tsx"
      provides: "heading block above the PodcastPlayer"
  key_links:
    - from: "docs/design.md"
      to: "all page files"
      via: "Page heading pattern (space-y-4 mb-12 block)"
      pattern: "text-\\[11px\\] uppercase tracking-\\[0.3em\\]"
---

<objective>
Apply the v3 standard heading pattern to every primary content page. Replace the old icon + text-subheading + count-badge pattern with the AI Studio mockup style: spaced overline label → large serif title → optional description.

Purpose: Every page needs this heading before the newspaper layout, archive redesign, and quiz restructure phases begin. The heading is the visual anchor that signals "this is a premium editorial product."
Output: 9 page files updated with consistent heading blocks.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/design.md

<interfaces>
<!-- CANONICAL HEADING PATTERN — copy this exactly for every page -->
<!-- From docs/design.md "Page heading (v3 standard)" -->

```tsx
<div className="space-y-4 mb-12">
  <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
    Overline Label
  </span>
  <h2 className="text-5xl font-serif">Page Title</h2>
  <p className="max-w-xl opacity-60 text-lg font-light">Optional description.</p>
</div>
```

Key details:
- tracking-[0.3em] — NOT tracking-widest. This is the very-wide spacing from the AI Studio mockup.
- text-5xl — large, not text-subheading (18px) or text-heading (24px)
- font-serif — Playfair Display
- opacity-40 on the overline — very subtle, not full opacity
- The description <p> is optional — omit for pages with dense content below
- The old pattern (icon + h2 + count badge) is REMOVED and replaced entirely
- DO NOT keep the icon or the count badge in the heading block (count badges may remain in filter chips below the heading, but not in the heading itself)
- mb-12 gives strong separation before the page content begins

Page-specific overlines and titles (editorial tone matching AI Studio mockup):

| Page | Overline | Title | Description |
|------|----------|-------|-------------|
| archive | Historical Record | Briefing Archive | Past editions, organised by date. |
| quiz | Intelligence Training | Daily Quiz | Test your commercial awareness. |
| firms | Directory | Law Firm Profiles | 46 firms. Magic Circle to boutique. |
| tests | Aptitude Preparation | Psychometric Tests | Watson Glaser and SJT practice. |
| primers | Practice Area Primers | Topic Primers | Eight areas. Essential for interviews. |
| events | Sector Intelligence | Legal Events | Upcoming events across UK legal markets. |
| saved | Your Library | Saved Articles | Bookmarked stories and notes. |
| interview | Interview Preparation | Practice Questions | Drawn from firm packs and sector primers. |
| podcast | Audio Briefing | Daily Briefing Podcast | The morning briefing, read aloud. |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add heading pattern to archive, quiz, and firms pages</name>
  <files>app/archive/page.tsx, app/quiz/page.tsx, app/firms/page.tsx</files>
  <action>
Read each file fully before editing.

**app/archive/page.tsx**
Find the current heading block (starts around line 48):
```tsx
<div className="flex items-center gap-3 mb-8">
  <Calendar size={16} className="text-stone-400" />
  <h2 className="text-subheading font-bold ...">Briefing Archive</h2>
  <span className="section-label ...">...</span>
</div>
```
Replace the entire `<div className="flex items-center gap-3 mb-8">` heading block with:
```tsx
<div className="space-y-4 mb-12">
  <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
    Historical Record
  </span>
  <h2 className="text-5xl font-serif">Briefing Archive</h2>
  <p className="max-w-xl opacity-60 text-lg font-light">Past editions, organised by date.</p>
</div>
```
Remove any Calendar import if no longer used elsewhere in the file (check before removing).

**app/quiz/page.tsx**
Find the heading block around line 177-183 (inside the `<main>` of the non-preview path):
```tsx
<div className="flex items-center gap-3 mb-8">
  <PenLine size={16} className="text-stone-400" />
  <h2 className="text-subheading font-bold ...">Daily Quiz</h2>
  ...
</div>
```
Replace with:
```tsx
<div className="space-y-4 mb-12">
  <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
    Intelligence Training
  </span>
  <h2 className="text-5xl font-serif">Daily Quiz</h2>
  <p className="max-w-xl opacity-60 text-lg font-light">Test your commercial awareness.</p>
</div>
```
There is also a preview/no-briefing branch earlier in the file — apply the same heading replacement there too if it has the same icon+title pattern. The hero card below the heading (bg-charcoal rounded-card) remains untouched.
Remove PenLine import only if no longer used elsewhere in the file.

**app/firms/page.tsx**
Find the heading block around line 20-26:
```tsx
<div className="flex items-center gap-3 mb-6">
  <Building2 size={16} className="text-stone-400" />
  <h2 className="text-subheading font-bold ...">Law Firms</h2>
  <span className="section-label ...">46</span>
</div>
```
Replace the entire block with:
```tsx
<div className="space-y-4 mb-12">
  <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
    Directory
  </span>
  <h2 className="text-5xl font-serif">Law Firm Profiles</h2>
  <p className="max-w-xl opacity-60 text-lg font-light">46 firms. Magic Circle to boutique.</p>
</div>
```
Remove Building2 import if unused elsewhere.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>archive, quiz, and firms pages each have the space-y-4 mb-12 heading block with overline + text-5xl serif title. Old icon+badge heading pattern removed from all three.</done>
</task>

<task type="auto">
  <name>Task 2: Add heading pattern to tests, primers, events, saved, interview, and podcast pages</name>
  <files>app/tests/page.tsx, app/primers/page.tsx, app/events/page.tsx, app/saved/page.tsx, app/interview/page.tsx, app/podcast/page.tsx</files>
  <action>
Read each file fully before editing.

**app/tests/page.tsx**
Find heading around line 18-27:
```tsx
<div className="flex items-center gap-3 mb-2">
  <GraduationCap size={16} className="text-stone-400" />
  <h2 className="text-lg font-bold ...">Aptitude Tests</h2>
  <span className="font-sans text-[10px] ...">2</span>
</div>
<p className="text-caption ... mb-2 ...">...</p>
<p className="text-caption ... mb-8 ...">...</p>
```
Replace the heading div AND both description `<p>` tags (they are old body text under the heading) with:
```tsx
<div className="space-y-4 mb-12">
  <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
    Aptitude Preparation
  </span>
  <h2 className="text-5xl font-serif">Psychometric Tests</h2>
  <p className="max-w-xl opacity-60 text-lg font-light">Watson Glaser and SJT practice.</p>
</div>
```
Remove GraduationCap import if unused elsewhere.

**app/primers/page.tsx**
Find heading around line 18-27:
```tsx
<div className="flex items-center gap-3 mb-2">
  <BookOpen size={16} className="text-stone-400" />
  <h2 className="text-lg font-bold ...">Sector Primers</h2>
  <span className="font-sans text-[10px] ...">8</span>
</div>
<p className="text-caption ... mb-8 ...">...</p>
```
Replace heading div and description p with:
```tsx
<div className="space-y-4 mb-12">
  <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
    Practice Area Primers
  </span>
  <h2 className="text-5xl font-serif">Topic Primers</h2>
  <p className="max-w-xl opacity-60 text-lg font-light">Eight areas. Essential for interviews.</p>
</div>
```
Remove BookOpen import if unused elsewhere.

**app/events/page.tsx**
The events page renders in two states (no-events fallback + events list). Find the heading block (line 58-62 in the main branch):
```tsx
<div className="flex items-center gap-3 mb-8">
  <Calendar size={16} className="text-stone-400" />
  <h1 className="font-bold text-heading text-stone-900 dark:text-stone-100">Events</h1>
  <span className="bg-stone-100 ... text-caption ...">N</span>
</div>
```
Replace with:
```tsx
<div className="space-y-4 mb-12">
  <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
    Sector Intelligence
  </span>
  <h1 className="text-5xl font-serif">Legal Events</h1>
  <p className="max-w-xl opacity-60 text-lg font-light">Upcoming events across UK legal markets.</p>
</div>
```
Apply the same heading replacement in the no-events fallback branch if it also has the icon+title pattern. Remove Calendar import only if unused (it may be used in event card date displays — check first).

**app/saved/page.tsx**
Find the heading block around line 17-19 (in the non-authenticated or main branch):
```tsx
<div className="flex items-center gap-3 mb-8">
  <Bookmark size={15} className="text-stone-400" />
  <h2 className="text-lg font-bold ...">Saved</h2>
```
Replace with:
```tsx
<div className="space-y-4 mb-12">
  <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
    Your Library
  </span>
  <h2 className="text-5xl font-serif">Saved Articles</h2>
  <p className="max-w-xl opacity-60 text-lg font-light">Bookmarked stories and notes.</p>
</div>
```
Remove Bookmark import if unused elsewhere.

**app/interview/page.tsx**
Find the heading block around line 57-62:
```tsx
<div className="flex items-center gap-3 mb-2">
  <MessageSquare size={16} className="text-stone-400" />
  <h2 className="text-lg font-bold ...">Interview Practice</h2>
  <span className="font-sans text-[10px] ...">40+</span>
</div>
<p className="text-[13px] ... mb-8 ...">...</p>
```
Replace heading div and description p with:
```tsx
<div className="space-y-4 mb-12">
  <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
    Interview Preparation
  </span>
  <h2 className="text-5xl font-serif">Practice Questions</h2>
  <p className="max-w-xl opacity-60 text-lg font-light">Drawn from firm packs and sector primers.</p>
</div>
```
Remove MessageSquare import if unused elsewhere.

**app/podcast/page.tsx**
The podcast page currently has no page-level heading — it goes directly into `<PodcastPlayer briefing={briefing} />`. Add the heading ABOVE the PodcastPlayer:
```tsx
<main className="max-w-3xl mx-auto px-4 sm:px-6 py-10">
  <div className="space-y-4 mb-12">
    <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
      Audio Briefing
    </span>
    <h1 className="text-5xl font-serif">Daily Briefing Podcast</h1>
    <p className="max-w-xl opacity-60 text-lg font-light">The morning briefing, read aloud.</p>
  </div>
  <PodcastPlayer briefing={briefing} />
  ...
</main>
```
No imports need to change for podcast.

After all edits, run TypeScript check.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 6 pages have the space-y-4 mb-12 heading block with overline + text-5xl serif title. TypeScript compiles clean. Old icon+badge heading pattern is removed from all pages.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:
1. `npx tsc --noEmit` — must pass with no errors
2. `grep -rn "text-5xl font-serif" /Users/gregoryvitrenko/Documents/Folio/app` — should show matches in all 9 page files
3. `grep -rn "tracking-\[0.3em\]" /Users/gregoryvitrenko/Documents/Folio/app` — should show matches in all 9 page files
4. `grep -rn "text-subheading font-bold\|text-lg font-bold.*tracking-tight\|text-heading.*stone-900" /Users/gregoryvitrenko/Documents/Folio/app/archive /Users/gregoryvitrenko/Documents/Folio/app/quiz /Users/gregoryvitrenko/Documents/Folio/app/firms /Users/gregoryvitrenko/Documents/Folio/app/tests /Users/gregoryvitrenko/Documents/Folio/app/primers /Users/gregoryvitrenko/Documents/Folio/app/events /Users/gregoryvitrenko/Documents/Folio/app/saved /Users/gregoryvitrenko/Documents/Folio/app/interview /Users/gregoryvitrenko/Documents/Folio/app/podcast` — should return zero matches (old heading pattern fully replaced)
</verification>

<success_criteria>
- All 9 primary content pages have the v3 heading pattern: overline (tracking-[0.3em]) + text-5xl font-serif title + optional description
- No page retains the old icon + text-subheading + count badge heading pattern as a primary page heading
- TypeScript compiles clean with no errors
- The editorial tone of overline labels matches the AI Studio mockup (not plain "Archive" or "Quiz" — editorial phrasing like "Historical Record", "Intelligence Training")
</success_criteria>

<output>
After completion, create `.planning/phases/25-accent-global-headings/25-02-SUMMARY.md`
</output>
