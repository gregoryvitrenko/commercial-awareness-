---
phase: 28-quiz-podcast-heroes-format
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/PodcastPlayer.tsx
autonomous: true
requirements:
  - POD-01

must_haves:
  truths:
    - "The podcast hero shows 'Daily Briefing Podcast' as a fixed title — no story headline from today's briefing appears there"
    - "The podcast hero description is evergreen — it does not reference a specific story or date"
    - "The player controls and waveform still work after the title change"
  artifacts:
    - path: "components/PodcastPlayer.tsx"
      provides: "Podcast hero block with evergreen copy"
      contains: "Daily Briefing Podcast"
  key_links:
    - from: "components/PodcastPlayer.tsx"
      to: "briefing prop"
      via: "briefing.date only (not briefing.stories[0].headline)"
      pattern: "briefing\\.date"
---

<objective>
Replace the dynamic episode title in the PodcastPlayer hero with fixed evergreen copy.

Purpose: The current hero uses `briefing.stories[0].headline` as the episode title and `briefing.stories[0].talkingPoints?.soundbite` as the subtitle — both change daily. POD-01 requires the hero to show "Daily Briefing Podcast" as a fixed title with a fixed description, decoupled from any specific story.

Output: Updated `components/PodcastPlayer.tsx` with evergreen hero copy.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/design.md

<interfaces>
<!-- Current dynamic values in PodcastPlayer.tsx that must be replaced -->

From components/PodcastPlayer.tsx (lines 464-465):
```typescript
const episodeTitle = briefing.stories[0]?.headline;
const episodeSubtitle = briefing.stories[0]?.talkingPoints?.soundbite ?? briefing.stories[0]?.talkingPoint;
```

These two consts drive the hero h1 and subtitle p. Both must be replaced with static strings.

The hero block starts at line 482 inside the render return. The left column (lines 494-563) contains:
- Overline row: "Latest Episode" chip + date
- h1 using `episodeTitle`
- subtitle p using `episodeSubtitle`
- Player controls row (unchanged)

The briefing prop is still needed for `briefing.date` (used in audio fetch URLs and the controls strip).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dynamic hero copy with evergreen title and description</name>
  <files>components/PodcastPlayer.tsx</files>
  <action>
In `components/PodcastPlayer.tsx`:

1. Delete the two derived constants (lines ~464-465):
   ```typescript
   const episodeTitle = briefing.stories[0]?.headline;
   const episodeSubtitle = briefing.stories[0]?.talkingPoints?.soundbite ?? briefing.stories[0]?.talkingPoint;
   ```

2. In the hero block, replace the overline "Latest Episode" chip with "Daily Briefing":
   ```tsx
   <span className="section-label bg-stone-800 text-stone-400 px-2 py-0.5">Daily Briefing</span>
   ```
   Keep the date chip next to it unchanged — it still uses `formatCoverDate(briefing.date)`.

3. Replace the h1 (currently `{episodeTitle ?? formatCoverDate(briefing.date)}`) with a fixed string:
   ```tsx
   <h1 className="font-serif text-3xl sm:text-4xl font-semibold text-stone-50 leading-tight tracking-tight">
     Daily Briefing Podcast
   </h1>
   ```

4. Replace the conditional subtitle paragraph (currently renders `{episodeSubtitle}` when truthy) with a fixed paragraph — always rendered, no condition:
   ```tsx
   <p className="text-sm text-stone-400 leading-relaxed max-w-lg">
     A 10-minute deep dive into the day&apos;s most critical legal news and commercial trends.
   </p>
   ```

5. In the decorative right cover card (lines ~567-581), update the brand text from "Folio Daily" to "Folio" and keep the "Intelligence Unit" / "Audio Briefing" labels unchanged.

Do NOT change: any player control logic, the waveform, the progress scrubber, speed control, download, the controls strip, the briefing notes panel, or any imports. The `briefing` prop is still used for `briefing.date` in audio fetch calls and the cover date label.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Hero h1 reads "Daily Briefing Podcast" — not a story headline
    - Subtitle is the fixed description string — never undefined, no story-specific content
    - TypeScript compiles with no new errors
    - `briefing.stories` is no longer referenced in the hero block (only in the briefing notes panel below)
  </done>
</task>

</tasks>

<verification>
After execution, confirm:
1. The podcast page hero title is "Daily Briefing Podcast" regardless of which briefing is loaded
2. The subtitle is the fixed evergreen description
3. Player controls (play/pause, waveform, scrubber, speed, download) still render and function
4. The briefing notes panel below the controls still shows all stories (unchanged)
5. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- "Daily Briefing Podcast" is the h1 in the hero for every briefing date
- The description "A 10-minute deep dive..." is always shown — never conditional
- No story headline from `briefing.stories[0]` appears in the hero area
- TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/28-quiz-podcast-heroes-format/28-01-SUMMARY.md`
</output>
