---
phase: 32-header-footer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/SiteFooter.tsx
autonomous: true
requirements:
  - SHELL-02

must_haves:
  truths:
    - "The footer shows the Folio wordmark at the left"
    - "The footer center shows a copyright + brand line"
    - "The footer right shows Legal / Privacy / Contact links"
    - "The footer appears on every page automatically (already wired in app/layout.tsx)"
  artifacts:
    - path: "components/SiteFooter.tsx"
      provides: "Three-zone footer: wordmark left, copyright center, nav links right"
      exports: ["SiteFooter"]
  key_links:
    - from: "app/layout.tsx"
      to: "components/SiteFooter.tsx"
      via: "Already imported and rendered as <SiteFooter /> in layout — no layout change needed"
      pattern: "SiteFooter"
---

<objective>
Redesign SiteFooter.tsx to match the mockup: Folio wordmark left, "© 2026 BRAND GUIDELINES" center, Legal / Privacy / Contact links right.

Purpose: The SiteFooter already exists and is already wired into app/layout.tsx. This plan is a targeted redesign of the component's internal layout and content to match the v3.2 mockup specification. No layout.tsx changes needed.

Output: A redesigned `components/SiteFooter.tsx` with three-zone horizontal layout — wordmark, copyright center text, and nav links.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current SiteFooter (already wired in layout — just needs redesign) -->

From app/layout.tsx:
```tsx
import { SiteFooter } from '@/components/SiteFooter';
// ...
<SiteFooter />  // rendered inside <Providers>, after <main>
```

From components/FolioLogo.tsx:
```typescript
export function FolioMark({ size?: number, className?: string }): JSX.Element
// Use size={20} or similar for footer wordmark — smaller than header
```

Current SiteFooter export signature (keep unchanged — no props):
```typescript
export function SiteFooter(): JSX.Element
```

Current SiteFooter layout (to be redesigned):
- Single row: "© {year} Folio" left, nav links right (Feedback, Terms, Privacy, Contact, LinkedIn)
- bg-stone-50 — needs to change to bg-paper for consistency
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign SiteFooter to three-zone mockup layout</name>
  <files>components/SiteFooter.tsx</files>
  <action>
Rewrite components/SiteFooter.tsx to match the v3.2 mockup footer spec.

**Three-zone layout (single row on desktop, stacked on mobile):**

Left zone — Folio wordmark:
- Import and render `FolioMark` from `./FolioLogo` at `size={18}`
- Place next to "olio" in `font-serif` matching header wordmark style
- Wrap in a `Link href="/"` with `flex items-center gap-1`
- Use `text-stone-400 dark:text-stone-500` for muted footer treatment

Center zone — copyright line:
- Text: `© 2026 BRAND GUIDELINES`
- Use `font-sans text-label uppercase tracking-widest text-stone-400 dark:text-stone-500`
- Note: hardcode `2026` — the existing dynamic `new Date().getFullYear()` approach is fine too, use judgment

Right zone — nav links:
- Legal: link to `/terms` (existing route)
- Privacy: link to `/privacy` (existing route)
- Contact: `mailto:hello@folioapp.co.uk`
- Typography: `font-sans text-label uppercase tracking-wide text-stone-400 dark:text-stone-500 hover:text-stone-700 dark:hover:text-stone-300 transition-colors`
- Render as `flex items-center gap-5`

**Outer container:**
```
<footer className="border-t border-stone-200 dark:border-stone-800 bg-paper mt-auto">
  <div className="max-w-5xl mx-auto px-4 sm:px-6 py-4 flex flex-col sm:flex-row items-center justify-between gap-4">
    {/* left: wordmark */}
    {/* center: copyright */}
    {/* right: nav links */}
  </div>
</footer>
```

On mobile (`flex-col`), stack vertically: wordmark top, copyright middle, links bottom. On `sm:flex-row` they spread horizontally with `justify-between`.

Remove from the current SiteFooter: Feedback link, LinkedIn link — keep only Legal / Privacy / Contact as specified in the phase requirements.

Do NOT add the `<SiteFooter />` to layout.tsx — it is already there. This task only modifies the component file.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>
    - TypeScript compiles with no errors
    - SiteFooter renders three zones: wordmark left, copyright center, Legal/Privacy/Contact right
    - bg-stone-50 replaced with bg-paper
    - Feedback and LinkedIn links removed
    - FolioMark imported and used for wordmark
    - No changes to app/layout.tsx (footer already wired)
  </done>
</task>

</tasks>

<verification>
After the task completes:

1. Run `npx tsc --noEmit` — zero errors
2. Confirm FolioMark is imported: `grep -n "FolioMark" components/SiteFooter.tsx`
3. Confirm bg-stone-50 is gone: `grep -n "bg-stone-50" components/SiteFooter.tsx` returns nothing
4. Confirm three links remain: `grep -n "terms\|privacy\|mailto" components/SiteFooter.tsx`
5. Confirm LinkedIn/Feedback removed: `grep -n "linkedin\|Feedback" components/SiteFooter.tsx` returns nothing
</verification>

<success_criteria>
- SiteFooter redesigned to three-zone layout: wordmark / copyright / nav links
- Visual language matches footer mockup (mono copyright, muted stone palette, paper background)
- Legal / Privacy / Contact links present; Feedback and LinkedIn removed
- SiteFooter still exported as `SiteFooter` with no props — layout.tsx requires no changes
- TypeScript build clean
</success_criteria>

<output>
After completion, create `.planning/phases/32-header-footer/32-02-SUMMARY.md` documenting:
- What changed in SiteFooter.tsx (zone layout, removed links, added FolioMark)
- Any design decisions (copyright year approach, mobile stacking behavior)
- TypeScript build result
</output>
