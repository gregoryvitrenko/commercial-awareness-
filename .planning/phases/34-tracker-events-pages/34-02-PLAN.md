---
phase: 34-tracker-events-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/events/page.tsx
  - app/events/CityFilter.tsx
autonomous: true
requirements:
  - EVENTS-01

must_haves:
  truths:
    - "Events page heading is centered: 'PROFESSIONAL NETWORK' overline + 'Upcoming Events' large serif + centered description"
    - "Each event card uses rounded-2xl corners — visibly large radius, clearly different from the standard rounded-card (2px)"
    - "Each event card has category chip (event type) top-left and a date string top-right"
    - "Each event card has a full-width outlined 'REGISTER INTEREST' button at the bottom"
    - "City filter tabs remain functional — filtering still works after redesign"
    - "Empty state page also shows the updated centered heading"
  artifacts:
    - path: "app/events/page.tsx"
      provides: "Events page with centered heading block"
      exports: ["EventsPage"]
    - path: "app/events/CityFilter.tsx"
      provides: "EventCard with rounded-2xl, category+date header row, REGISTER INTEREST button"
      exports: ["CityFilter", "EventsGrid"]
  key_links:
    - from: "app/events/CityFilter.tsx"
      to: "app/events/[id]/page.tsx"
      via: "Link href={/events/${event.id}}"
      pattern: "Link.*events.*event.id"
---

<objective>
Redesign the events page heading to be centered at a large serif scale, and redesign the EventCard to match the mockup: rounded-2xl corners, category chip top-left + date top-right in a flex row, and a full-width outlined "REGISTER INTEREST" link button at the bottom.

Purpose: Events is a free-tier growth feature — the redesigned cards should feel premium and inviting, matching the mockup's rounded-card aesthetic.
Output: Updated app/events/page.tsx (heading) and app/events/CityFilter.tsx (EventCard component).
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/design.md
</context>

<interfaces>
<!-- Current structure the executor must work from — no codebase exploration needed -->

From app/events/page.tsx — current heading block (appears in both empty-state and main renders):
```tsx
<div className="space-y-4 mb-12">
  <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
    Sector Intelligence
  </span>
  <h1 className="text-5xl font-serif">Legal Events</h1>
  <p className="max-w-xl opacity-60 text-lg font-light">Upcoming events across UK legal markets.</p>
</div>
```

From app/events/CityFilter.tsx — current EventCard:
```tsx
function EventCard({ event }: EventCardProps) {
  return (
    <Link
      href={`/events/${event.id}`}
      className="block rounded-card border border-stone-200 dark:border-stone-800 hover:border-stone-400 dark:hover:border-stone-600 transition-colors p-4"
    >
      <div className="flex items-start justify-between gap-2 mb-2">
        <span className={`inline-block rounded-chrome px-2 py-0.5 text-label font-medium ${EVENT_TYPE_COLOURS[event.eventType]}`}>
          {event.eventType}
        </span>
      </div>
      <h3 className="text-body font-semibold text-stone-900 dark:text-stone-100 mb-1 leading-snug">
        {event.title}
      </h3>
      <p className="text-caption text-charcoal dark:text-charcoal-light font-medium mb-1">
        {formatShortDate(event.date, event.time)}
      </p>
      <p className="text-caption text-stone-500">
        {event.city} · {event.organiser}
      </p>
      <p className="section-label mt-2 text-stone-500">
        {event.eligibility}
      </p>
    </Link>
  );
}
```

LegalEvent type fields available: id, title, date, time?, city, organiser, eligibility, eventType, registrationUrl?, description?

EVENT_TYPE_COLOURS map already exists — keep it.
formatShortDate already exists — keep it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update events page heading to centered large serif</name>
  <files>app/events/page.tsx</files>
  <action>
Update BOTH heading blocks in app/events/page.tsx (the empty-state render and the main render — they are identical and must both be updated).

Replace the current heading block with:

```tsx
{/* Page heading — centered */}
<div className="text-center space-y-3 mb-12">
  <span className="section-label opacity-40">Professional Network</span>
  <h1 className="text-[64px] leading-none font-serif font-bold text-stone-900 dark:text-stone-50">
    Upcoming Events
  </h1>
  <p className="opacity-60 text-lg font-light max-w-xl mx-auto">
    Curated events across UK legal markets — networking, workshops, and career fairs.
  </p>
</div>
```

Also remove the `← Home` Link from both renders (lines 17–19 and 54–56 in the current file). The new header shell (Phase 32) handles navigation — back links are no longer needed on individual pages.

Keep everything else in page.tsx unchanged: dynamic export, getEvents call, upcoming filter, Suspense wrapper, CityFilter and EventsGrid usage.
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript clean. Both heading blocks updated to centered layout. "PROFESSIONAL NETWORK" overline + "Upcoming Events" ~64px serif + centered description. Back links removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign EventCard — rounded-2xl, date top-right, REGISTER INTEREST button</name>
  <files>app/events/CityFilter.tsx</files>
  <action>
Replace the EventCard function in CityFilter.tsx with the following redesigned version. Keep all other exports (EventsGrid, CityFilter, EVENT_TYPE_COLOURS, formatShortDate) unchanged.

The EventCard is no longer a Link wrapper around the whole card. Instead the card is a div with a Link button at the bottom. This allows the "REGISTER INTEREST" button to be a distinct interactive element.

```tsx
function formatDateDisplay(dateStr: string): string {
  const [year, month, day] = dateStr.split('-').map(Number);
  return new Date(year, month - 1, day).toLocaleDateString('en-GB', {
    day: 'numeric',
    month: 'short',
    year: 'numeric',
  }).toUpperCase();
}

function EventCard({ event }: EventCardProps) {
  return (
    <div className="flex flex-col rounded-2xl border border-stone-200 dark:border-stone-800 bg-white dark:bg-stone-900 p-5 gap-3">
      {/* Top row: category chip left, date right */}
      <div className="flex items-center justify-between gap-2">
        <span className={`inline-block rounded-chrome px-2 py-0.5 text-label font-medium ${EVENT_TYPE_COLOURS[event.eventType]}`}>
          {event.eventType}
        </span>
        <span className="section-label text-stone-400 dark:text-stone-500 whitespace-nowrap">
          {formatDateDisplay(event.date)}
        </span>
      </div>

      {/* Title */}
      <h3 className="font-serif text-[18px] font-semibold text-stone-900 dark:text-stone-100 leading-snug">
        {event.title}
      </h3>

      {/* Time + location */}
      <p className="text-caption text-stone-500 dark:text-stone-400">
        {event.time ? `${event.time} · ` : ''}{event.city} · {event.organiser}
      </p>

      {/* Spacer pushes button to bottom */}
      <div className="flex-1" />

      {/* REGISTER INTEREST — full-width outlined button */}
      <Link
        href={event.registrationUrl ?? `/events/${event.id}`}
        target={event.registrationUrl ? '_blank' : undefined}
        rel={event.registrationUrl ? 'noopener noreferrer' : undefined}
        className="block w-full text-center border border-stone-300 dark:border-stone-600 rounded-full py-2.5 section-label text-stone-700 dark:text-stone-300 hover:border-stone-500 dark:hover:border-stone-400 hover:text-stone-900 dark:hover:text-stone-100 transition-colors"
      >
        REGISTER INTEREST
      </Link>
    </div>
  );
}
```

Note: `formatDateDisplay` is a new helper added alongside the existing `formatShortDate`. Both can coexist — `formatShortDate` may still be used elsewhere in the file (city filter display). Add `formatDateDisplay` just before the `EventCard` function.

If `registrationUrl` does not exist on the LegalEvent type in lib/types.ts, fall back to `/events/${event.id}` only (use `event.registrationUrl ?? \`/events/${event.id}\``). If TypeScript errors on `registrationUrl`, check lib/types.ts LegalEvent interface and add it as an optional field: `registrationUrl?: string;` — but only if it is truly missing.

The EventsGrid div wrapper stays identical: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
  </action>
  <verify>
    <automated>cd /Users/gregoryvitrenko/Documents/Folio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compiles clean. EventCard renders with rounded-2xl corners. Category chip top-left, date string top-right. Title in font-serif. Full-width outlined "REGISTER INTEREST" link at card bottom. City filter tabs still functional.
  </done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — must return 0 errors.
Visual check on /events (dev server): heading centered with large serif "Upcoming Events". Cards show rounded-2xl corners clearly. Each card has type chip top-left, date top-right. "REGISTER INTEREST" button spans full card width as outlined pill at bottom. City filter tabs still filter cards correctly.
</verification>

<success_criteria>
1. Events heading: "PROFESSIONAL NETWORK" section-label overline + "Upcoming Events" ~64px serif bold, text-center + centered description
2. EventCard: rounded-2xl border, white bg, category chip top-left, date top-right, serif title, time/location row, full-width outlined "REGISTER INTEREST" button at bottom
3. Both empty-state and main render use the updated centered heading
4. City filter tabs continue to work (no regression)
5. TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/34-tracker-events-pages/34-02-SUMMARY.md` using the standard summary template.
</output>
