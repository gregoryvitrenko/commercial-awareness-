---
phase: 36-quiz-page-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/quiz/page.tsx
autonomous: true
requirements:
  - QUIZ-01
must_haves:
  truths:
    - "The quiz page has no date archive list (AVAILABLE section with past dates)"
    - "The gamification stats strip (XP/level/streak) appears as its own row at the top, before the two quiz panels"
    - "The quiz page shows exactly two panels side by side at desktop: Daily (left) and Deep Practice (right)"
    - "On mobile the two panels stack vertically, Daily first"
    - "The page heading (overline + serif title) and the gamification strip are in separate rows — not crammed into one flex row"
  artifacts:
    - path: "app/quiz/page.tsx"
      provides: "Quiz hub page — server component with heading, stats strip, and two-panel layout"
      contains: "statsStrip rendered above the grid div, not inside the heading flex row"
  key_links:
    - from: "app/quiz/page.tsx"
      to: "getGamificationData(userId)"
      via: "Promise.all in QuizPage server component"
      pattern: "getGamificationData"
    - from: "statsStrip"
      to: "two-panel grid"
      via: "DOM order — statsStrip div before grid div"
      pattern: "statsStrip.*grid|grid.*statsStrip"
---

<objective>
Restructure the quiz page layout so the gamification stats strip (XP/level/streak) renders as a dedicated full-width row at the very top of the page content — above the two-panel quiz grid — rather than inline beside the heading text.

Purpose: The current layout puts the stats strip in a `flex items-start justify-between` row alongside the heading text. This makes the heading and stats compete for horizontal space and buries the stats beside the heading rather than surfacing them prominently. The target mockup has the stats as a distinct top section.

Output: A revised `app/quiz/page.tsx` where the page renders in three clear vertical stacks: (1) heading block, (2) gamification stats strip (its own row, full width), (3) two-panel grid (Daily left, Deep Practice right). No date archive list anywhere on the page.
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/quiz/page.tsx
@lib/quiz-gamification.ts
</context>

<interfaces>
<!-- Key types the executor needs. No codebase exploration required. -->

From lib/quiz-gamification.ts:
```typescript
export interface GamificationData {
  xp: number;
  level: number;
  streak: number;
  lastCompleted: string | null;
}

export async function getGamificationData(userId: string): Promise<GamificationData>
```

Current quiz page structure (app/quiz/page.tsx):
- Server component, calls `requireSubscription()` and `auth()`
- Fetches `briefing` + `gamification` in `Promise.all`
- `statsStrip` JSX is computed as a variable (conditional on `gamification !== null`)
- Main return: heading flex row (heading + statsStrip side by side) → two-panel grid
- No date archive list in current code — already removed in Phase 28/31
- Two-panel grid: `lg:col-span-7` Daily card (dark `bg-[#1B2333]`) + `lg:col-span-5` Deep Practice list
- Deep Practice topics are a static array `DEEP_PRACTICE_TOPICS` (8 items)
- `formatDisplayDate` helper for date formatting
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restructure quiz page — separate heading and stats strip into distinct rows</name>
  <files>app/quiz/page.tsx</files>
  <action>
    Rewrite the layout structure in `app/quiz/page.tsx` so the page renders in three vertically stacked sections instead of the current two-row layout.

    Current structure (to change):
    ```
    <div className="flex items-start justify-between gap-8 mb-12 flex-wrap">
      <div className="space-y-3">  ← heading
        ...
      </div>
      {statsStrip}  ← stats crammed beside heading
    </div>
    <div className="grid grid-cols-1 lg:grid-cols-12 gap-8">
      ... two panels ...
    </div>
    ```

    Target structure:
    ```
    {/* 1. Heading block */}
    <div className="space-y-3 mb-6">
      <span className="section-label opacity-40">Intelligence Training</span>
      <h2 className="text-5xl font-serif text-stone-900 dark:text-stone-50">Commercial Quiz</h2>
    </div>

    {/* 2. Gamification strip — own row, full width, before panels */}
    {statsStrip && (
      <div className="mb-10">
        {statsStrip}
      </div>
    )}

    {/* 3. Two-panel grid */}
    <div className="grid grid-cols-1 lg:grid-cols-12 gap-8">
      ... unchanged panels ...
    </div>
    ```

    Also update the `statsStrip` JSX variable: change the outer wrapper from `<div className="flex items-center gap-3 flex-shrink-0">` to `<div className="flex items-center gap-4 flex-wrap">` so the two stat cards (streak + XP/level) lay out horizontally and wrap on narrow viewports rather than staying rigidly `flex-shrink-0`.

    Apply the same three-section restructure to BOTH the early-return branch (no briefing) AND the main return branch. The `statsStrip` variable is defined once above both returns so it applies to both.

    Do NOT change:
    - The dark `bg-[#1B2333]` Daily quiz card content or its Link href (`/quiz/${activeDate}`)
    - The Deep Practice topic list content, links, or styling
    - `requireSubscription()`, `auth()`, `Promise.all` data fetching
    - `DEEP_PRACTICE_TOPICS` array
    - `formatDisplayDate` helper
    - The individual stat card JSX inside `statsStrip` (streak dots, XP bar, level display)
    - Any imports

    Confirm there is no date archive list (AVAILABLE section) anywhere in the file — if found, remove it. Based on the current code it is already absent.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compiles without errors. The quiz page file has: (1) heading block in its own div with mb-6, (2) gamification strip in its own div with mb-10 rendered between heading and grid, (3) two-panel grid unchanged. The statsStrip wrapper uses flex-wrap not flex-shrink-0. No date list present anywhere in the file.
  </done>
</task>


</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- No `flex-shrink-0` on the statsStrip wrapper
- No date archive list (no "AVAILABLE" label or past-date list) in `app/quiz/page.tsx`
- statsStrip div appears in DOM order BEFORE the two-panel grid div
- Both early-return branch and main return branch have the correct three-section structure
</verification>

<success_criteria>
1. The date archive list (AVAILABLE section with list of past quiz dates) is absent from the quiz page
2. The gamification stats strip (XP, level, streak) appears as a dedicated row above the two quiz panels — not inline beside the heading
3. The quiz page shows exactly two panels side by side at desktop: Daily (left, lg:col-span-7) and Deep Practice (right, lg:col-span-5)
4. The layout is clean and uncluttered — three clear vertical sections: heading / stats / panels
</success_criteria>

<output>
After completion, create `.planning/phases/36-quiz-page-cleanup/36-quiz-page-cleanup-01-SUMMARY.md` summarising:
- What changed in `app/quiz/page.tsx`
- The final DOM order of the three sections
- Any deviations from the plan and why
</output>
