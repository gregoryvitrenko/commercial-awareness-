---
phase: 37-tests-interview-fit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/TestCard.tsx
autonomous: true
requirements:
  - TESTS-01
must_haves:
  truths:
    - "Each test card shows a gray icon square at the top (stone-100 background, icon inside)"
    - "Each test card shows a bulleted list of what the test measures (from test.subtypes)"
    - "The charcoal 'Start practising' CTA button remains at the bottom of each card"
    - "No Clock or BarChart2 meta row is visible — removed in favour of feature bullets"
  artifacts:
    - path: "components/TestCard.tsx"
      provides: "Redesigned test card with icon square + feature bullets"
      exports: ["TestCard"]
  key_links:
    - from: "components/TestCard.tsx"
      to: "lib/tests-data.ts"
      via: "test.subtypes array"
      pattern: "test\\.subtypes"
---

<objective>
Redesign the TestCard component to match the mockup: a gray icon square at the top, a bullet list of what the test measures (from subtypes), and the charcoal CTA button. Remove the Clock + BarChart2 meta row.

Purpose: Tests page cards should communicate what each test measures at a glance, not just metadata.
Output: Updated components/TestCard.tsx
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From lib/tests-data.ts:
```typescript
export interface TestMeta {
  slug: 'watson-glaser' | 'sjt';
  name: string;
  shortName: string;
  vendor: string;
  strapline: string;
  description: string;
  usedBy: string[];
  subtypes: { name: string; description: string }[];
  tips: string[];
  timeNote: string;
  difficulty: 'Moderate' | 'Hard';
}
```

Watson Glaser has 5 subtypes: Inference, Recognition of Assumptions, Deduction, Interpretation, Evaluation of Arguments.
SJT has 1 subtype: Most Effective Response.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign TestCard with gray icon square and feature bullets</name>
  <files>components/TestCard.tsx</files>
  <action>
Rewrite components/TestCard.tsx to match the mockup design:

1. GRAY ICON SQUARE (top of card, before vendor overline):
   - `<div className="w-10 h-10 bg-stone-100 dark:bg-stone-800 rounded-lg flex items-center justify-center mb-4">`
   - Use `Brain` icon (size 18, className="text-stone-500 dark:text-stone-400") for watson-glaser
   - Use `Users` icon (size 18, className="text-stone-500 dark:text-stone-400") for sjt
   - Determine icon by checking `test.slug === 'watson-glaser'` inline
   - Import both `Brain` and `Users` from 'lucide-react' (remove `Clock`, `BarChart2`, `ChevronRight`)

2. VENDOR OVERLINE: keep `<p className="section-label text-stone-400 dark:text-stone-500 mb-2">{test.vendor}</p>`

3. TITLE: keep `<h2 className="font-serif text-2xl font-bold tracking-tight text-stone-900 dark:text-stone-50 leading-snug mb-3">{test.name}</h2>`

4. STRAPLINE (replace description): `<p className="text-caption text-stone-500 dark:text-stone-400 leading-relaxed mb-4">{test.strapline}</p>`

5. FEATURE BULLETS (replaces meta row + usedBy):
   - Section label: `<p className="section-label text-stone-400 dark:text-stone-500 mb-2">What it measures</p>`
   - Bullet list: `<ul className="space-y-1 mb-6">` mapping over `test.subtypes.slice(0, 4)`
   - Each bullet: `<li className="flex items-start gap-2 text-[12px] text-stone-600 dark:text-stone-400">`
   - Bullet marker: `<span className="mt-1 w-1 h-1 rounded-full bg-stone-400 flex-shrink-0" />`
   - Text: `<span>{subtype.name}</span>`
   - If subtypes.length > 4, add a final li showing `+ {subtypes.length - 4} more` in same style

6. SPACER: `<div className="flex-1" />`

7. CTA BUTTON: keep as-is — `<div className="inline-flex items-center gap-2 bg-charcoal text-white font-semibold text-sm px-5 py-3 rounded-chrome group-hover:bg-charcoal-light transition-colors">Start practising <ArrowRight size={14} /></div>`
   - Import `ArrowRight` from 'lucide-react' instead of `ChevronRight`

The outer Link wrapper, card classes, and hover behaviour stay unchanged.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>TestCard renders gray icon square, "What it measures" bullet list from subtypes, and charcoal CTA. Clock/BarChart2 meta row gone. TypeScript clean.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — zero errors relating to TestCard.tsx.
Visually confirm on /tests page: each card has a gray square with icon at top, bullet list of subtype names, charcoal "Start practising" button.
</verification>

<success_criteria>
- TestCard has gray icon square (Brain for Watson Glaser, Users for SJT)
- Feature bullets list subtype names under "What it measures" label
- Clock/BarChart2 meta row removed
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/37-tests-interview-fit/37-01-SUMMARY.md`
</output>
