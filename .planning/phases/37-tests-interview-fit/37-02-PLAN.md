---
phase: 37-tests-interview-fit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/interview/page.tsx
  - app/fit/page.tsx
autonomous: true
requirements:
  - INTVW-01
  - FIT-01
must_haves:
  truths:
    - "Interview category cards have no colored backgrounds — all use white/stone-900 with stone border"
    - "Each interview category card shows a gray icon square (not a colored accent element)"
    - "The 'By Practice Area' section (divider, paragraph, primer grid, tips banner) is entirely removed from /interview"
    - "Visiting /fit shows a landing page with two cards: Firm Fit and Area Fit"
    - "Each fit card links to the correct quiz route (/firm-fit and /area-fit) without breaking those existing routes"
  artifacts:
    - path: "app/interview/page.tsx"
      provides: "Cleaned interview page — gray icon squares, no colored backgrounds, no By Practice Area section"
    - path: "app/fit/page.tsx"
      provides: "New fit landing page with two cards"
  key_links:
    - from: "app/fit/page.tsx"
      to: "/firm-fit"
      via: "Link href"
      pattern: "href.*firm-fit"
    - from: "app/fit/page.tsx"
      to: "/area-fit"
      via: "Link href"
      pattern: "href.*area-fit"
---

<objective>
Two changes: (1) redesign interview page category cards to remove colored backgrounds and add gray icon squares, and remove the entire "By Practice Area" section below. (2) Create a new /fit landing page that shows two cards — Firm Fit and Area Fit — linking to the existing quiz routes.

Purpose: The interview page is cleaner and more consistent with the stone palette. The fit assessment has a proper entry point that explains both quiz options before committing.
Output: Updated app/interview/page.tsx, new app/fit/page.tsx
</objective>

<execution_context>
@/Users/gregoryvitrenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregoryvitrenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From lib/interview-data.ts:
```typescript
export interface InterviewCategory {
  slug: InterviewCategorySlug;  // 'strengths' | 'behavioural' | 'motivation' | 'commercial'
  name: string;
  shortName: string;
  description: string;
  strapline: string;
  color: string;  // 'emerald' | 'blue' | 'violet' | 'amber' — being REMOVED from card styling
  usedBy: string;
  tips: string[];
}
```

Current interview page uses COLOR_MAP keyed by cat.color to set bg, border, badge, text classes.
Target: all cards use `bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800`.

Category slugs → suitable lucide icons:
- strengths → Zap (energy, what comes naturally)
- behavioural → Users (people/competencies)
- motivation → Target (goal/purpose)
- commercial → TrendingUp (market awareness)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove colored card styles from interview page, add gray icon squares, remove By Practice Area section</name>
  <files>app/interview/page.tsx</files>
  <action>
Rewrite app/interview/page.tsx with these changes:

REMOVE entirely:
- The `COLOR_MAP` constant (lines 16-41)
- The `PRIMERS` import from '@/lib/primers-data'
- The `TOPIC_STYLES` import from '@/lib/types'
- The entire "Practice area divider" section and everything after it (the divider div, description paragraph, practice area grid, tips banner) — the page ends after the category grid closing `</div>`

ADD icon map at the top of the file (after imports):
```typescript
import { Zap, Users, Target, TrendingUp } from 'lucide-react';

const CATEGORY_ICONS: Record<string, React.ComponentType<{ size?: number; className?: string }>> = {
  strengths: Zap,
  behavioural: Users,
  motivation: Target,
  commercial: TrendingUp,
};
```

UPDATE each category card (the Link element inside the map):
- Remove: `${colors.border} ${colors.bg}` — replace with: `border-stone-200 dark:border-stone-800 bg-white dark:bg-stone-900`
- Remove the entire header row div that contains `colors.text` and `colors.badge`
- Add BEFORE the header row: a gray icon square:
  ```tsx
  {(() => {
    const Icon = CATEGORY_ICONS[cat.slug] ?? Zap;
    return (
      <div className="w-10 h-10 bg-stone-100 dark:bg-stone-800 rounded-lg flex items-center justify-center mb-3 flex-shrink-0">
        <Icon size={18} className="text-stone-500 dark:text-stone-400" />
      </div>
    );
  })()}
  ```
- Replace the colored category shortName label (`colors.text`) with plain stone text:
  `<p className="section-label text-stone-400 dark:text-stone-500 mb-1">{cat.shortName}</p>`
- Replace the colored badge count with plain stone:
  `<span className="flex-shrink-0 section-label text-stone-400 dark:text-stone-500 bg-stone-100 dark:bg-stone-800 px-2 py-0.5 rounded">{count}</span>`
- Replace the CTA line (`colors.text`):
  `<div className="flex items-center gap-1 text-[12px] font-medium mt-auto text-stone-600 dark:text-stone-300 group-hover:gap-2 transition-all">`

Also remove the `ArrowRight` import from 'lucide-react' if still needed — keep it since the CTA still uses it.

The page heading block (text-center, overline + h2 + description) stays unchanged.
The category grid div stays (grid grid-cols-1 sm:grid-cols-2 gap-4) stays unchanged.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>Interview page renders 4 category cards with gray icon squares, stone borders, no colored backgrounds. By Practice Area section gone. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create /fit landing page with two cards</name>
  <files>app/fit/page.tsx</files>
  <action>
Create a new file app/fit/page.tsx — a server component, no auth required (fit quizzes are free):

```tsx
import type { Metadata } from 'next';
import Link from 'next/link';
import { Building2, Scale } from 'lucide-react';
import { Header } from '@/components/Header';
import { getTodayDate } from '@/lib/storage';

export const metadata: Metadata = {
  title: 'Fit Assessment · Folio',
  description: 'Find the right firm and practice area for you. Take the Firm Fit quiz to discover which law firm matches your style, or the Area Fit quiz to find your ideal practice area.',
};

export const dynamic = 'force-dynamic';

export default async function FitPage() {
  const today = getTodayDate();

  return (
    <>
      <Header date={today} />
      <main className="max-w-5xl mx-auto px-4 sm:px-6 py-8">

        {/* Page heading */}
        <div className="space-y-4 mb-12 text-center">
          <span className="text-[11px] uppercase tracking-[0.3em] font-semibold opacity-40 font-sans">
            Self Assessment
          </span>
          <h2 className="text-5xl font-serif">Fit Assessment</h2>
          <p className="opacity-60 text-lg font-light">
            Find the right firm and practice area for you.
          </p>
        </div>

        {/* Two-card grid */}
        <div className="grid grid-cols-1 sm:grid-cols-2 gap-6 max-w-2xl mx-auto">

          {/* Firm Fit card */}
          <Link
            href="/firm-fit"
            className="group block bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 rounded-card p-6 hover:border-stone-300 dark:hover:border-stone-600 transition-colors flex flex-col"
          >
            <div className="w-10 h-10 bg-stone-100 dark:bg-stone-800 rounded-lg flex items-center justify-center mb-4">
              <Building2 size={18} className="text-stone-500 dark:text-stone-400" />
            </div>
            <p className="section-label text-stone-400 dark:text-stone-500 mb-2">2 minutes</p>
            <h3 className="font-serif text-2xl font-bold tracking-tight text-stone-900 dark:text-stone-50 leading-snug mb-3">
              Firm Fit Quiz
            </h3>
            <p className="text-caption text-stone-500 dark:text-stone-400 leading-relaxed mb-6">
              Discover which type of law firm matches your personality, ambitions, and working style. Get personalised recommendations from 38 profiles.
            </p>
            <div className="flex-1" />
            <div className="inline-flex items-center gap-2 bg-charcoal text-white font-semibold text-sm px-5 py-3 rounded-chrome group-hover:bg-charcoal-light transition-colors">
              Start quiz
            </div>
          </Link>

          {/* Area Fit card */}
          <Link
            href="/area-fit"
            className="group block bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 rounded-card p-6 hover:border-stone-300 dark:hover:border-stone-600 transition-colors flex flex-col"
          >
            <div className="w-10 h-10 bg-stone-100 dark:bg-stone-800 rounded-lg flex items-center justify-center mb-4">
              <Scale size={18} className="text-stone-500 dark:text-stone-400" />
            </div>
            <p className="section-label text-stone-400 dark:text-stone-500 mb-2">2 minutes</p>
            <h3 className="font-serif text-2xl font-bold tracking-tight text-stone-900 dark:text-stone-50 leading-snug mb-3">
              Area Fit Quiz
            </h3>
            <p className="text-caption text-stone-500 dark:text-stone-400 leading-relaxed mb-6">
              Find out which legal practice area matches your working style and interests. M&A, disputes, banking, regulation, AI and more.
            </p>
            <div className="flex-1" />
            <div className="inline-flex items-center gap-2 bg-charcoal text-white font-semibold text-sm px-5 py-3 rounded-chrome group-hover:bg-charcoal-light transition-colors">
              Start quiz
            </div>
          </Link>

        </div>
      </main>
    </>
  );
}
```

Note: /firm-fit and /area-fit routes are left completely unchanged — they still render FirmQuiz and AreaQuiz directly. The /fit route is purely additive.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>/fit page exists, renders two cards linking to /firm-fit and /area-fit. TypeScript clean. Existing /firm-fit and /area-fit routes unmodified.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — zero errors.
Check /interview: 4 cards with gray icon squares, stone borders, no colored backgrounds, no By Practice Area section below.
Check /fit: landing page with "Firm Fit Quiz" and "Area Fit Quiz" cards, both linking correctly.
Check /firm-fit and /area-fit still work (no regressions).
</verification>

<success_criteria>
- Interview page has no COLOR_MAP, no colored card backgrounds, gray icon squares on all 4 category cards
- By Practice Area divider, description, primer grid, and tips banner all removed from /interview
- /fit route exists as a new landing page with two charcoal-CTA cards
- /firm-fit and /area-fit remain unchanged
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/37-tests-interview-fit/37-02-SUMMARY.md`
</output>
